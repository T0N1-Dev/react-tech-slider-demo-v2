# Apply Progress: Build Interactive Slider Showcase

## Task 1.1 — Dependency and test harness bootstrap

- **BOOTSTRAP:** `pnpm add react-tech-slider@^1.9.3` — passed; registry package 1.9.3 installed.
- **BOOTSTRAP:** `pnpm add -D vitest jsdom @testing-library/react @testing-library/user-event @testing-library/jest-dom` — passed.
- Added Vitest/jsdom configuration, Testing Library cleanup, jest-dom, and a deterministic non-reduced-motion `matchMedia` shim. No sentinel test was created.
- **VERIFY:** `pnpm list --depth 0` — passed; resolved `react-tech-slider@1.9.3`, React/React DOM 19.2.7, Vitest 4.1.10, jsdom 29.1.1, and the approved Testing Library dependencies without peer errors.
- **VERIFY:** `pnpm exec vitest --version` — passed; `vitest/4.1.10 win32-x64 node-v22.16.0`.
- **VERIFY:** `pnpm exec tsc --noEmit` — passed with zero diagnostics.
- **TYPE RESOLUTION:** inspected `node_modules/react-tech-slider/package.json` and `dist/index.d.ts`/`dist/types.d.ts`: the published root export maps `types` to `./dist/index.d.ts`, which exports `Slider`, `Brand`, `RunningSliderProps`, `FadesSliderProps`, and `SliderProps`. The declarations expose the approved discriminated props and peers accept React 19 (`react ^19.1.0`; `react-dom` includes `^19.0.0`). No package source was edited.
- **CONFIG REVIEW:** `package.json` contains `test`/`test:watch`; `vitest.config.ts` uses jsdom and the explicit setup file; `src/test/setup.ts` registers jest-dom, cleanup, and only deterministic `matchMedia` support. No disallowed tooling or sentinel test is present.

## Task 2.1 — Canonical state, normalization, and invocation

- **RED:** `pnpm exec vitest run src/showcase/model.test.ts` — intended failure before production code: Vite could not resolve `./model`; suite failed before collecting tests. The first assertion uses a local `Brand[]` fixture and expects the exact default running invocation entries. Changed file: `src/showcase/model.test.ts`.
- **GREEN:** `pnpm exec vitest run src/showcase/model.test.ts` — passed 1 file/1 test (`projects the default running settings into exact public entries`). Added the minimum typed defaults and running projection. Changed file: `src/showcase/model.ts`.
- **TRIANGULATE RED:** `pnpm exec vitest run src/showcase/model.test.ts` — intended behavioral expansion failed 26/29 tests: missing normalization/color/reducer/dataset-selection APIs and fades still projected as running. The initial numeric matrix covered every domain but did not apply every counterexample category to each domain; that coverage gap is corrected in the Checkpoint A remediation below. Also added injectable CSS-color helper cases, documented/reduced-motion defaults, field-specific actions, separate-store round trips, atomic reset, and exact running/fades key assertions. Changed file: `src/showcase/model.test.ts`.
- **TRIANGULATE GREEN:** `pnpm exec vitest run src/showcase/model.test.ts` — passed 1 file/29 tests after implementing finite clamp/quantization, color validation, captured-reset reducer, active configuration/dataset selection, and exhaustive discriminated invocation projection. Changed file: `src/showcase/model.ts`.
- **REFACTOR:** centralized reducer numeric extraction through `normalizedValue` while retaining public normalization results, discriminated types, and `projectSlider` as the single prop-selection authority. Exact invocation-key parity assertions cover running `[iconWidth, borderWidth, borderColor, backgroundColor, isPlay, pauseOnHoverActive, durationMs]` and fades `[iconWidth, gap, speed]`, with explicit forbidden running `speed`, `autoPlay`, and `pauseOnHover` checks.
- **REFACTOR VERIFY:** `pnpm exec vitest run src/showcase/model.test.ts` — passed 1 file/29 tests; `pnpm test` — passed 1 file/29 tests; `pnpm build` — passed (`tsc -b`, Vite 8.1.5, 15 modules); `pnpm lint` — passed with zero warnings/errors.

## Task 3.1 — Canonical invocation code generation

- **RED:** `pnpm exec vitest run src/showcase/codegen.test.ts src/showcase/model.test.ts` — intended failure: Vite could not resolve absent `./codegen`; model remained green (29 tests). Added an exact minimal running snippet and entry-identity/name parity assertions. Changed file: `src/showcase/codegen.test.ts`.
- **GREEN:** `pnpm exec vitest run src/showcase/codegen.test.ts src/showcase/model.test.ts` — passed 2 files/31 tests after adding the smallest invocation-to-code-model renderer and exact source formatter. Changed file: `src/showcase/codegen.ts`.
- **TRIANGULATE RED:** `pnpm exec vitest run src/showcase/codegen.test.ts` — intended expansion failed 4/9 tests: non-default running/fades entries remained omitted, canonical brand dimensions were absent, and the first cold compiler-API fixture exceeded Vitest's 5-second default timeout. Root-import, forbidden-name, default fades omission, and one generated fixture already passed. Added Node types to `tsconfig.app.json` for the compiler-API test and removed its JSON comments so the edited config is strict JSON. Changed files: `src/showcase/codegen.test.ts`, `tsconfig.app.json`.
- **TRIANGULATE GREEN:** `pnpm exec vitest run src/showcase/codegen.test.ts` — passed 1 file/9 tests after moving package-default emission decisions into canonical model entries, rendering scalar types without prop-name selection, preserving canonical brand width/height, and allowing 15 seconds for cold TypeScript compiler startup. Representative `generated-running.tsx` and `generated-fades.tsx` compiled in memory against the installed package-root declarations with zero diagnostics. Changed files: `src/showcase/model.ts`, `src/showcase/codegen.ts`, `src/showcase/codegen.test.ts`.
- **REFACTOR:** `codegen.ts` now only transports the invocation discriminant, canonical brands, and the exact invocation `props` array; it filters the model-owned emission rule and formats values generically. Prop-name/default selection remains exclusively in `model.ts`; no transport fallback URL or CSS/package-internal import is present.
- **REFACTOR VERIFY:** `pnpm exec vitest run src/showcase/codegen.test.ts src/showcase/model.test.ts` — passed 2 files/38 tests; `pnpm test` — passed 2 files/38 tests; `pnpm build` — passed (`tsc -b`, Vite 8.1.5, 15 modules, production bundle emitted); `pnpm lint` — passed with zero warnings/errors. Compiler fixtures: `generated-running.tsx`, `generated-fades.tsx`; diagnostics: 0 for each.

## Task 3.2 — Checkpoint A: model/source contract

- **RELIABILITY REVIEW — no blockers:** `src/showcase/model.ts` is the only public-prop/default-emission authority. Its running entries exactly match installed `RunningSliderProps` (`iconWidth`, `borderWidth`, `borderColor`, `backgroundColor`, `isPlay`, `pauseOnHoverActive`, `durationMs`), and fades entries exactly match installed `FadesSliderProps` (`iconWidth`, `gap`, `speed`) with the discriminant added from the canonical invocation. The numeric domains and inclusive clamp/step behavior match the specification; tests cover empty, NaN, Infinity, below/above-range, and quantization cases for every domain.
- **RELIABILITY REVIEW — no blockers:** running omits only exact package defaults, fades omits only `speed={1}`, while controlled `iconWidth={5}` and `gap={48}` remain emitted. Exact/parity/forbidden-name tests cover running and fades, and both representative generated TSX fixtures resolve the published package root with zero compiler diagnostics. The lockfile resolves `react-tech-slider@1.9.3` against React/React DOM 19.2.7.
- **READABILITY REVIEW — no blockers:** `toCodeModel` retains the canonical `props` array by identity, and `renderSliderCode` performs only generic emission/formatting plus discriminant rendering. There is no second preview or code prop-name mapping in the current slice, no React import in the pure model/codegen modules, and no package-internal/CSS-subpath or fallback-state leakage. Future preview work must iterate the same invocation entries rather than introduce a mapping.
- **CHECKPOINT RESULT (SUPERSEDED):** initially recorded as passed, then reopened after review confirmed unchecked reducer color commits, incomplete per-domain normalization evidence, and an exported uncorrelated code-renderer bypass.

## Checkpoint A remediation — Tasks 2.1, 3.1, and 3.2 reopened

- **RED:** `pnpm exec vitest run src/showcase/model.test.ts src/showcase/codegen.test.ts` — failed 3/62 tests as intended. `rejects invalid color commits before canonical state and invocation` proved `not-a-color` entered reducer state; `exposes only the canonical invocation renderer at runtime` found exported `renderSliderCode`/`toCodeModel`; and `rejects the exported uncorrelated renderer bypass at type level` compiled a fades-plus-running-props model with zero diagnostics. Changed tests: `src/showcase/model.test.ts`, `src/showcase/codegen.test.ts`.
- **TRIANGULATE TEST EXPANSION:** replaced the partial numeric table with 40 explicit combinations: minimum, maximum, empty, NaN, positive Infinity, below range, above range, and off-step quantization for each of `iconWidth`, `borderWidth`, `durationMs`, `gap`, and `speed`.
- **GREEN:** `pnpm exec vitest run src/showcase/model.test.ts src/showcase/codegen.test.ts` — passed 2 files/62 tests after injecting the color validator into `createPlaygroundReducer`, normalizing both field-specific color commits before state entry, removing the exported uncorrelated code model/renderer, and making the private renderer accept `SliderInvocation` directly. Changed files: `src/showcase/model.ts`, `src/showcase/codegen.ts`.
- **TRIANGULATE GREEN:** added a compiler-API case proving `generateSliderCode` rejects a fades discriminant paired with a running `borderWidth` entry, alongside the export-surface runtime assertion and exported-bypass type test. `pnpm exec vitest run src/showcase/model.test.ts src/showcase/codegen.test.ts` — passed 2 files/63 tests; all 40 numeric matrix cases and the invalid-color state/invocation assertion passed. Changed file: `src/showcase/codegen.test.ts`.
- **REFACTOR:** centralized reducer color extraction in `normalizedColorValue`; retained unchanged field-specific action names. `codegen.ts` now exports only `generateSliderCode`, while its private renderer preserves the `SliderInvocation` discriminant/props correlation and generically iterates canonical entries.
- **VALIDATION RED/FIX:** the first concurrent `pnpm test` run failed 1/63 tests because a cold compiler-API negative fixture exceeded Vitest's 5-second default; production behavior, build, and lint were green. Applied the same explicit 15-second allowance already used by representative generated-TSX compilation to both negative compiler fixtures, then reran without concurrency.
- **REFACTOR VERIFY:** `pnpm exec vitest run src/showcase/model.test.ts src/showcase/codegen.test.ts` — passed 2 files/63 tests; `pnpm test` — passed 2 files/63 tests; `pnpm build` — passed (`tsc -b`, Vite 8.1.5, 15 modules); `pnpm lint` — passed with zero warnings/errors. Representative `generated-running.tsx` and `generated-fades.tsx` still compile against published declarations with zero diagnostics.

## Checkpoint A remediation review

- **RELIABILITY — no blockers:** `src/showcase/model.ts` requires an injected validator when constructing the reducer; invalid/empty border and background commits retain prior canonical values, while valid field-specific commits still pass. The canonical invocation test proves invalid strings cannot reach projected props.
- **RELIABILITY — no blockers:** the 5 domains × 8 counterexample categories matrix covers exact inclusive boundaries, empty, NaN, Infinity, below/above clamp, and step quantization. Domains remain exactly `iconWidth 1–10/0.25`, `borderWidth 0–8/1`, `durationMs 5000–120000/1000`, `gap 0–180/4`, and `speed 0.25–4/0.25`.
- **RELIABILITY — no blockers:** installed public declarations still match the discriminated invocation entry sets. The only codegen export accepts `SliderInvocation`; compiler tests reject both the removed uncorrelated bypass and fades paired with running entries, while representative valid running/fades TSX produces zero diagnostics.
- **READABILITY — no blockers:** validation stays centralized in pure normalization helpers, reducer actions remain field-specific, prop/default authority remains in `projectSlider`, and the private code renderer only iterates correlated canonical entries. No independent preview/code prop mapping, CSS subpath, fallback state, or task-4 brand implementation was introduced.
- **CHECKPOINT RESULT:** passed after RED-led remediation; tasks 2.1, 3.1, and 3.2 may be re-checked.

## Task 4.1 — Canonical brand fixtures, validation, and fallbacks

- **RED:** `pnpm exec vitest run src/showcase/brands.test.ts` — intended missing-module failure: Vite could not resolve absent `./brands`; no tests collected. The first tests require both fixtures to validate with finite unique IDs and require deterministic readable SVG fallback output. Changed file: `src/showcase/brands.test.ts`.
- **GREEN:** `pnpm exec vitest run src/showcase/brands.test.ts` — passed 1 file/3 tests after adding two six-brand HTTPS technology fixtures, the minimum empty/valid validation result, and a deterministic readable SVG data URL. Changed file: `src/showcase/brands.ts`.
- **TRIANGULATE RED:** `pnpm exec vitest run src/showcase/brands.test.ts src/showcase/model.test.ts` — intended expansion failed 12/70 tests: eight invalid counterexamples were accepted, fallback list materialization was absent, and model projection incorrectly treated empty/invalid validation results as ready datasets. Added empty-vs-invalid, duplicate/non-finite ID, blank name, empty/non-HTTPS/malformed URL, over-six cap, multi-brand readable fallback, identity/order/dimension preservation, and model bypass/canonical-data assertions. Changed files: `src/showcase/brands.test.ts`, `src/showcase/model.test.ts`.
- **TRIANGULATE GREEN:** `pnpm exec vitest run src/showcase/brands.test.ts src/showcase/model.test.ts` — passed 2 files/70 tests after enforcing the six-item limit, finite unique IDs, non-empty trimmed names, HTTPS URL parsing, XML-safe initials/name fallbacks, identity-preserving fallback materialization, and empty/invalid/ready projection results. Changed files: `src/showcase/brands.ts`, `src/showcase/model.ts`.
- **REFACTOR:** centralized and cached deterministic fallback URL construction by stable brand identity without obscuring validation branches. Both curated fixtures contain exactly 6 brands (12 total). Validation counterexamples: empty, duplicate ID, NaN ID, infinite ID, blank name, empty URL, HTTP URL, malformed URL, and 7-item input.
- **REFACTOR VERIFY:** `pnpm exec vitest run src/showcase/brands.test.ts src/showcase/model.test.ts` — passed 2 files/70 tests; `pnpm test` — passed 3 files/81 tests; `pnpm build` — passed (`tsc -b`, Vite 8.1.5, 15 modules); `pnpm lint` — passed with zero warnings/errors.

## Task 5.1 — Image preflight and terminal late-failure recovery

- **RED:** `pnpm exec vitest run src/showcase/useResolvedBrands.test.tsx` — intended missing-hook failure: Vite could not resolve absent `./useResolvedBrands`; no tests collected. The first per-test fake `Image` assertion requires pending fallback materialization followed by decode-backed canonical success. Changed file: `src/showcase/useResolvedBrands.test.tsx`.
- **GREEN:** `pnpm exec vitest run src/showcase/useResolvedBrands.test.tsx` — passed 1 file/1 test after adding the minimum pending-fallback resolver and decode-backed canonical success transition. Changed file: `src/showcase/useResolvedBrands.ts`.
- **TRIANGULATE RED:** `pnpm exec vitest run src/showcase/useResolvedBrands.test.tsx` — intended expansion failed 4/11 tests: duplicate URLs created two preflights, equivalent rerenders retried a terminal failure, and the late-failure reporter was absent. Added one/all failure, decode rejection, URL deduplication, stale generation cleanup, terminal no-retry, identity/order/dimension preservation, matching/unrelated/fallback late failure, and canonical-code invariance cases. Changed file: `src/showcase/useResolvedBrands.test.tsx`.
- **TRIANGULATE GREEN:** `pnpm exec vitest run src/showcase/useResolvedBrands.test.tsx` — passed 1 file/11 tests after deduplicating canonical URLs, keying generations by the distinct URL set, terminally storing preflight/decode/late failures, detaching stale handlers, and exposing a matching-ready-only reporter. Changed file: `src/showcase/useResolvedBrands.ts`.
- **REFACTOR REVIEW RED:** an added dataset-round-trip assertion failed 1/12 tests: returning to settled failed/ready URLs created four `Image` instances instead of preserving one preflight per distinct URL. Changed file: `src/showcase/useResolvedBrands.test.tsx`.
- **REFACTOR:** retained settled URL status across dataset generations in a hook-local cache, mirrored through render state without reading refs during render, and centralized defensive URL-key parsing. Browser transport remains outside `model.ts`; no fetch, canvas, blob, proxy, timer, or automatic retry path exists.
- **REFACTOR VERIFY:** `pnpm exec vitest run src/showcase/useResolvedBrands.test.tsx src/showcase/brands.test.ts` — passed 2 files/28 tests; `pnpm test` — passed 4 files/93 tests; `pnpm build` — passed (`tsc -b`, Vite 8.1.5, 15 modules); `pnpm lint` — passed with zero warnings/errors.
- **EVENT/RETRY EVIDENCE:** pending → fallback; load → decode resolve → canonical; error/decode reject → terminal fallback; canonical-ready late match → terminal fallback; unrelated/fallback late URLs → ignored. Duplicate/settled URLs create one `Image` each, failed URLs remain at one attempt across equivalent rerenders and dataset round trips, and stale detached callbacks leave the current generation on fallback. Identity/order/width/height remain stable, while canonical arrays and generated source retain original HTTPS URLs.

## Tasks 4.1–5.1 review

- **RELIABILITY — no blockers:** fixtures are bounded to six recognizable HTTPS brands each; validation distinguishes empty from invalid and rejects every specified identity/name/URL/cap counterexample before projection. Fallback SVG data is deterministic, XML-escaped, readable, and identity-preserving.
- **RESILIENCE — no blockers:** image work is distinct-URL deduplicated, generation-guarded, decode-aware, terminal on failure, and free of retry timers. Late reporting mutates only matching canonical-ready transport status; unrelated and data URLs are ignored.
- **READABILITY — no blockers:** `brands.ts` owns pure validation/fallback policy, `useResolvedBrands.ts` owns browser-only URL state, and `model.ts` owns invocation projection. Canonical code generation never consumes resolved/fallback URLs.
- **SCOPE — no blockers:** no UI component, task 6 file, sibling package, old demo, package source, or unrelated dependency was changed.

## Task 6.1 — Reusable clipboard feedback

- **RED:** `pnpm exec vitest run src/showcase/components/CopyButton.test.tsx` — intended missing-component failure: Vite could not resolve absent `./CopyButton`; no tests collected. The first assertion requires the exact supplied string to reach `writeText` and a visible polite success announcement. Changed file: `src/showcase/components/CopyButton.test.tsx`.
- **GREEN:** after placing the per-test clipboard mock after `userEvent.setup()` (which installs its own clipboard stub), `pnpm exec vitest run src/showcase/components/CopyButton.test.tsx` passed 1 file/1 test. Added the minimum exact-write, retained `<code>`, native button, and polite success message. Changed files: `src/showcase/components/CopyButton.tsx`, `src/showcase/components/CopyButton.test.tsx`.
- **TRIANGULATE RED:** `pnpm exec vitest run src/showcase/components/CopyButton.test.tsx` — intended expansion failed 4/8 tests with three expected unhandled failures: rejected, absent, and synchronous clipboard paths produced no manual-copy feedback; source lacked the selectable focus hook; and no success timeout existed to clean up. Independent instances and Enter/Space native activation already passed. Changed file: `src/showcase/components/CopyButton.test.tsx`.
- **TRIANGULATE GREEN:** `pnpm exec vitest run src/showcase/components/CopyButton.test.tsx` — passed 1 file/8 tests after implementing local discriminated status, rejected/absent/synchronous failure handling, retained focusable `<code>`, request isolation, and a cleaned 2-second success timeout. Changed file: `src/showcase/components/CopyButton.tsx`.
- **REFACTOR REVIEW RED:** a React `StrictMode` assertion failed 1/9 tests because the simulated cleanup left the mounted guard false. Resetting the guard in each effect setup fixed development-runtime behavior without changing ownership.
- **REFACTOR VERIFY:** `pnpm exec vitest run src/showcase/components/CopyButton.test.tsx` — passed 1 file/9 tests; `pnpm test` — passed 5 files/102 tests; `pnpm build` — passed (`tsc -b`, Vite 8.1.5, 15 modules); `pnpm lint` — passed with zero warnings/errors. Clipboard outcomes: exact resolve → “Copied to clipboard.”; reject/absent/synchronous throw → “Copy failed. Select and copy the text manually.”; no false success; source remains visible and focusable; each instance owns status; unmount leaves zero timers.

## Task 6.2 — Install constants and automatic WAI-ARIA tabs

- **RED:** `pnpm exec vitest run src/showcase/components/InstallGuide.test.tsx` — intended missing-component failure: Vite could not resolve absent `./InstallGuide`; no tests collected. The first assertion requires npm selected by default, exact visible `npm install react-tech-slider`, and copying that identical string. Changed file: `src/showcase/components/InstallGuide.test.tsx`.
- **GREEN:** `pnpm exec vitest run src/showcase/components/InstallGuide.test.tsx` — passed 1 file/1 test after adding centralized package/URL/command/preset constants and minimal local npm/pnpm selection, active panel, and `CopyButton` integration. Changed files: `src/showcase/constants.ts`, `src/showcase/components/InstallGuide.tsx`.
- **TRIANGULATE RED:** `pnpm exec vitest run src/showcase/components/InstallGuide.test.tsx src/showcase/components/CopyButton.test.tsx` — intended expansion failed 5/18 tests: resource links were absent; tabs lacked roving `tabIndex` and panel associations; Arrow/Home/End did not focus or automatically select; and no visible non-color cue existed. Exact pnpm click/copy, native Enter/Space, clipboard rejection, and all CopyButton tests already passed. Changed file: `src/showcase/components/InstallGuide.test.tsx`.
- **TRIANGULATE GREEN:** `pnpm exec vitest run src/showcase/components/InstallGuide.test.tsx src/showcase/components/CopyButton.test.tsx` — passed 2 files/18 tests after implementing labeled automatic tabs, roving focus, Arrow wrapping, Home/End endpoints, tab/panel IDs, visible “✓ Selected” cues, and package/repository links. Changed file: `src/showcase/components/InstallGuide.tsx`.
- **REFACTOR:** package identity, canonical URLs, exact command grammar, and 960/768/390 preview widths remain centralized in `constants.ts`. `InstallGuide` owns only local package-manager selection and remounts command-local copy feedback when selection changes; no install or clipboard state enters the playground model.
- **REFACTOR VERIFY:** `pnpm exec vitest run src/showcase/components/InstallGuide.test.tsx src/showcase/components/CopyButton.test.tsx` — passed 2 files/18 tests; `pnpm test` — passed 6 files/111 tests; `pnpm build` — passed (`tsc -b`, Vite 8.1.5, 15 modules); `pnpm lint` — passed with zero warnings/errors. Copied strings: exact `npm install react-tech-slider` and `pnpm add react-tech-slider`. Keyboard paths: ArrowRight npm→pnpm→npm wrap, ArrowLeft npm→pnpm wrap, End→pnpm, Home→npm, plus native Enter/Space activation.

## Tasks 6.1–6.2 review

- **RELIABILITY — no blockers:** every clipboard failure mode produces manual-copy feedback without hiding or changing source, successful feedback is instance-local and timer-cleaned, and StrictMode behavior is covered. Commands are copied from exact constants only.
- **ACCESSIBILITY/READABILITY — no blockers:** the automatic tab set has a labeled tablist, selected state, roving `tabIndex`, focus movement, controls/tabpanel association, native buttons, and visible non-color selection text. The active panel is labeled by its selected tab and retains visible focusable command text.
- **OWNERSHIP/SCOPE — no blockers:** install selection stays in `InstallGuide`; copy status stays in each `CopyButton`; neither appears in `model.ts`. No playground, preview, page, stylesheet, task 7 file, sibling package, or old demo was changed.

## Task 7.1 — Variant controls, state ownership, and generated source

- **RED:** `pnpm exec vitest run src/showcase/components/Playground.test.tsx` — intended missing-component failure: Vite could not resolve absent `./Playground`; no tests collected. The first test injects only the downstream preview boundary and requires default running controls plus generated source from the canonical invocation. Changed file: `src/showcase/components/Playground.test.tsx`.
- **GREEN:** `pnpm exec vitest run src/showcase/components/Playground.test.tsx` — passed 1 file/1 test after adding minimum playground reducer ownership, running controls, dispatch wiring, dataset projection, generated-code display/copy, reset, and an injected preview boundary. Changed files: `src/showcase/components/Playground.tsx`, `src/showcase/components/ControlPanel.tsx`, `src/showcase/components/GeneratedCode.tsx`.
- **TRIANGULATE RED:** `pnpm exec vitest run src/showcase/components/Playground.test.tsx src/showcase/model.test.ts src/showcase/codegen.test.ts` — intended expansion failed all 8 new playground scenarios (model/codegen remained green): missing running color/toggle and fades controls, no committed-valid local drafts, no limitation note, and direct numeric dispatch caused premature clamp/reset. Added exact ranges/units, inactive absence, fades source, invalid drafts, normalization, round-trip stores, viewport invariance, reduced-motion pause, atomic reset/focus, and install-selection persistence cases. Changed file: `src/showcase/components/Playground.test.tsx`.
- **TRIANGULATE GREEN:** `pnpm exec vitest run src/showcase/components/Playground.test.tsx src/showcase/model.test.ts src/showcase/codegen.test.ts` — passed 3 files/73 tests after adding local committed-valid numeric/color drafts, all running/fades controls, inactive subtree unmounting, shared/separate-store round trips, exact motion disclosure, and preview-only viewport dispatch. Changed files: `src/showcase/components/ControlPanel.tsx`, `src/showcase/components/Playground.tsx`.
- **REFACTOR:** field draft/validation logic remains in focused `NumericField`/`ColorField` helpers inside `ControlPanel`; `Playground` alone owns reducer state/projection; `GeneratedCode` only receives a completed string. No field logic entered `App`, no prop mapping entered controls, and no source interpretation entered the display component.
- **REFACTOR VERIFY:** focused task files passed 3 files/73 tests; `pnpm test` passed 7 files/119 tests; `pnpm build` passed (`tsc -b`, Vite 8.1.5, 15 modules); `pnpm lint` passed with zero warnings/errors. Control names: Demonstration dataset, Icon width (rem), Border width (px), Border color, Background color, Playback, Pause on hover, Duration (ms), Gap (px), Speed (×). Focus targets: selected variant radio and Reset playground button. Source parity remained covered by model/codegen tests; viewport/install state stayed absent from source.

## Task 8.1 — Published preview materialization and data states

- **RED:** `pnpm exec vitest run src/showcase/components/Playground.test.tsx` — intended missing-preview behavior: 1/9 tests failed because the default boundary still rendered “Live preview pending” and never invoked the mocked package-root `Slider`. The new assertion requires exactly canonical running entries plus pending resolved `brandsList`, with no running discriminator. Changed file: `src/showcase/components/Playground.test.tsx`.
- **GREEN:** `pnpm exec vitest run src/showcase/components/Playground.test.tsx` — passed 1 file/9 tests after adding `toPreviewProps` entry iteration, published root `Slider` rendering, pending resolved-brand substitution, and making `PreviewPanel` the default boundary. Changed files: `src/showcase/model.ts`, `src/showcase/components/PreviewPanel.tsx`, `src/showcase/components/Playground.tsx`.
- **TRIANGULATE RED:** `pnpm exec vitest run src/showcase/components/Playground.test.tsx src/showcase/useResolvedBrands.test.tsx` — intended expansion failed 1/26 tests: the requested empty/invalid dataset seam was ignored and the package still received the default fixture. Added exact fades props/forbidden keys, data bypass/guidance, preset metadata/source invariance, matching/unrelated late failures, and all-fallback usability; all transport/preset cases already passed. Changed file: `src/showcase/components/Playground.test.tsx`.
- **TRIANGULATE GREEN:** focused preview/transport tests passed 2 files/26 tests after adding the bounded dataset seam and explicit `brandsList` guidance. Exact running call keys: `brandsList`, `iconWidth`, `borderWidth`, `borderColor`, `backgroundColor`, `isPlay`, `pauseOnHoverActive`, `durationMs`; exact fades keys: `variant`, `brandsList`, `iconWidth`, `gap`, `speed`. Empty and duplicate-ID data bypassed `Slider`.
- **REFACTOR:** `toPreviewProps` remains the only invocation-entry iterator for package props; `PreviewPanel` performs only resolved URL substitution, discriminant materialization, data-state rendering, preset metadata, and image-error forwarding. No independent prop-name map was introduced.
- **REFACTOR VERIFY:** focused preview/transport/model files passed 3 files/80 tests; `pnpm test` passed 7 files/125 tests; `pnpm build` passed (`tsc -b`, Vite 8.1.5, 15 modules); `pnpm lint` passed with zero warnings/errors. Presets produced 960/768/390 metadata without source changes; matching canonical image failure changed only that URL to fallback; unrelated/fallback failures were ignored; all-fallback preview remained usable.

## Task 8.2 — Package-only error boundary and recovery

- **RED:** `pnpm exec vitest run src/showcase/components/PreviewErrorBoundary.test.tsx` — intended missing-component failure: Vite could not resolve absent `./PreviewErrorBoundary`; no tests collected. Expected console suppression is scoped to the throwing-boundary test. The assertion requires a local “Live preview unavailable” alert with both recovery actions. Changed file: `src/showcase/components/PreviewErrorBoundary.test.tsx`.
- **GREEN:** `pnpm exec vitest run src/showcase/components/PreviewErrorBoundary.test.tsx` — passed 1 file/1 test after adding the minimum class boundary and local alert/actions. Changed file: `src/showcase/components/PreviewErrorBoundary.tsx`.
- **TRIANGULATE RED:** `pnpm exec vitest run src/showcase/components/PreviewErrorBoundary.test.tsx src/showcase/components/Playground.test.tsx` — intended integration expansion failed 3/18 tests with uncaught package errors: no narrow boundary surrounded `Slider`, so repeated containment, current-state retry, reset-and-retry, stable focus, and surrounding-content survival were unavailable. Changed file: `src/showcase/components/Playground.test.tsx`.
- **TRIANGULATE GREEN:** boundary/playground files passed 2 files/18 tests after wrapping only the package/capture subtree with a retry-keyed class boundary. Retry remounted current normalized props; reset-and-retry restored captured defaults; both focused the stable `Live preview` heading. Repeated throwing remained a local alert.
- **REFACTOR:** dedicated retry state remains local to `Playground` and outside state/source models. `PreviewPanel` keeps heading, presets, canvas, and data states outside the keyed boundary; only the package render and late-image capture container are guarded.
- **REFACTOR VERIFY:** focused boundary/playground files passed 2 files/18 tests; `pnpm test` passed 8 files/129 tests; `pnpm build` passed (`tsc -b`, Vite 8.1.5, 15 modules); `pnpm lint` passed with zero warnings/errors. Fallback scope: preview package subtree only. Ordinary variant/viewport changes during failure produced no package remount; dedicated retry did. Controls, generated source, install tabs/links, and fades disclosure remained reachable. Recovery focus landed on `Live preview`.

## Task 8.3 — Checkpoint B review and RED-led blockers

- **CHECKPOINT RED:** `pnpm exec vitest run src/showcase/components/Playground.test.tsx` — failed 2/18 tests. Reliability review found that reset did not clear an invalid local draft when its committed value already equaled the initial value. Resilience/adoption review found empty/invalid generated guidance was a sentence rather than copy-ready canonical package-root TSX. Changed file: `src/showcase/components/Playground.test.tsx`.
- **BLOCKERS RESOLVED:** normal reset and reset-and-retry now remount only control-local drafts while preserving atomic model reset; empty/invalid `InvocationResult` now carries copy-ready package-root `Brand[]`/`Slider` TSX. Focused playground/model tests passed 2 files/72 tests after the owning-slice fixes.
- **REMOTE/PREVIEW RELIABILITY — no blockers:** preflight remains pending-fallback-first, distinct-URL deduplicated, decode-aware, generation-cleaned, and terminal with no settled URL retries. Late capture reports only matching canonical-ready package image URLs; unrelated/data URLs are ignored. Empty/invalid data bypasses `Slider`; all-fallback data remains usable.
- **INTERACTION/RESILIENCE — no blockers:** clipboard denial retains source/manual guidance; controls cover exact domains and committed-valid drafts; variant stores/shared data/source remain synchronized; reset clears model and draft state while install selection remains independent; fades has the exact no-pause/full-reduced-motion disclosure.
- **ERROR CONTAINMENT — no blockers:** the class boundary guards only the package/capture subtree. Repeated failure stays local; only retry key remounts it; current-state retry and captured-state reset/retry restore the preview and focus the stable heading. Controls, source, install guidance/links, and disclosure survive failure.
- **CHECKPOINT VERIFY (SUPERSEDED):** selected checkpoint files passed 7 files/114 tests; `pnpm test` passed 8 files/130 tests; `pnpm build` passed; `pnpm lint` passed. A subsequent fresh checkpoint reopened tasks 6.1, 6.2, 7.1, and 8.3 for the gaps below.

## Fresh Checkpoint B remediation

- **RED:** `pnpm exec vitest run src/showcase/components/CopyButton.test.tsx src/showcase/components/InstallGuide.test.tsx src/showcase/components/Playground.test.tsx` — failed 9/36 tests as intended. Corrected numeric commits lacked nearby range/unit feedback; copy buttons/announcements did not identify install command versus generated code; reset had no completion announcement; and invalid-data alert omitted the validator's specific safe reason. Changed tests: `CopyButton.test.tsx`, `InstallGuide.test.tsx`, `Playground.test.tsx`.
- **GREEN:** Added required copy target labels and target-specific polite success/manual-copy failure announcements; added nearby normalized-value/range/unit correction feedback without marking corrected values invalid; added a repeatable polite reset-completion status while retaining reset-button focus; and surfaced the validator-owned invalid-data reason alongside canonical guidance. The focused command passed 3 files/36 tests.
- **TRIANGULATE:** Correction cases cover step rounding (`7.12` to `7 rem`, `82` to `84 px`) and maximum clamping (`999999` to `120000 ms`) across shared/running/fades controls. Copy cases cover install-command and generated-code success/failure plus absent/synchronous/rejected clipboard paths. Invalid data retains the package-root guidance code while reporting the specific duplicate-ID reason. The seven checkpoint files passed 7 files/114 tests.
- **REFACTOR:** Numeric draft state now records the canonical value on which an edit began, so external/reset values resynchronize without a state-setting effect while post-commit correction feedback remains mounted. Copy feedback remains instance-local, reset feedback remains outside canonical invocation state, and preview/source projection was not duplicated. `pnpm test` passed 8 files/130 tests; `pnpm build` passed (`tsc -b`, Vite 8.1.5, 15 modules); `pnpm lint` passed with zero warnings/errors.
- **CHECKPOINT B REPEAT:** Reliability/resilience review found no remaining blockers: terminal image-resolution behavior and canonical URL separation remain unchanged; clipboard targets and denial guidance are specific; empty/invalid states bypass package rendering and retain canonical code; the package boundary/retry key remains narrow; reset announces completion without moving focus; normalized controls remain canonical and source-correlated; install selection remains independent; and the fades limitation remains exact. Tasks 6.1, 6.2, 7.1, and 8.3 were re-checked. Task 9.1 was not started.

## Task 9.1 — One-page evaluation and adoption flow

- **RED:** Added `src/App.test.tsx` with three semantic composition cases for banner/main/contentinfo landmarks, ordered headings, a focusable same-page playground target, live `Playground`/`InstallGuide` wiring, exact package resources, package-root guidance, source-before-controls order, and repeated fades disclosure. `pnpm exec vitest run src/App.test.tsx` failed 3/3 as intended against the placeholder `Hola Mundillo` app.
- **GREEN:** Replaced the placeholder with one banner/main/contentinfo flow, wired the existing `Playground` and `InstallGuide`, added exact root-import and variant guidance, retained source-before-controls order, and repeated the exact fades limitation. `pnpm exec vitest run src/App.test.tsx` passed 3/3.
- **TRIANGULATE:** Added a package-throw integration case proving install/API guidance survives the narrow preview boundary, plus exact document-order and non-goal assertions. `pnpm exec vitest run src/App.test.tsx src/showcase/components/Playground.test.tsx` passed 2 files/22 tests.
- **REFACTOR:** Kept one-use hero, API, variant, and footer content inline in `App.tsx`; package constants and interactive behavior remain in their existing owners. The final shared full-suite/build/lint evidence is recorded under checkpoint C below.

## Task 9.2 — Dark editorial responsive CSS

- **RED:** Added stable-hook and stylesheet-contract cases for the shell, playground, preview presets, generated-code containment, controls, all three preview widths, exact 768/390 breakpoints, focus visibility, and prohibitions on hidden horizontal layout or package animation overrides. The first command exposed a test-only `import.meta.url` assumption under Vitest; the path was corrected to `process.cwd()` before accepting evidence. The valid `pnpm exec vitest run src/App.test.tsx src/showcase/components/Playground.test.tsx` RED failed 2/24 solely for the missing hooks and missing `src/styles.css`.
- **GREEN:** Added presentation hooks and a tokenized dark-editorial stylesheet imported by `main.tsx`. The wide playground is a `minmax(0, …)` two-column grid with preview/source before controls in DOM order; 960px and below becomes one column. Native controls and primary actions have 44px minimum targets, focus-visible outlines, wrapped preset/tab groups, non-color selected/invalid/corrected/success/paused cues, bounded preview canvases, and horizontally scrollable code regions. The focused command passed 2 files/24 tests.
- **TRIANGULATE RED/GREEN:** Added desktop/tablet/mobile attribute transitions, long-source containment selectors, wrapped-group/min-width contracts, invalid/paused/copy-success states, empty/invalid/error presentation states, and fallback-count metadata. The focused command failed 1/25 for the missing fallback hook, then passed 2 files/25 tests after adding terminal-fallback count metadata and a dashed non-color canvas cue. CSS contains no `overflow-x: hidden` layout concealment and no package animation override.
- **REFACTOR:** Consolidated palette/spacing/radius tokens and grouped shell, playground, control, copy, preview-state, install, and API selectors without adding a component or CSS framework. Automated jsdom/source checks do not prove pixels, rendered contrast, or package animation behavior. No browser engine was available in this execution, so 1440/1024/768/390 visual inspection remains explicitly manual rather than claimed.

## Task 9.3 — Checkpoint C review

- **READABILITY / SCOPE:** `App.tsx` retains static one-use hero/API/footer sections inline and composes only the existing `Playground` and `InstallGuide`; no route, state mapping, new dependency, package-source edit, or CSS framework was introduced. Package and repository links remain centralized exact constants.
- **RELIABILITY / SOURCE ORDER:** Preview and canonical generated source remain before controls in DOM order, while the wide grid changes presentation only. Generated source still comes solely from `projectSlider` plus `generateSliderCode`; CSS/preset/fallback metadata does not enter invocation state. The selected checkpoint command passed 5 files/44 tests.
- **ACCESSIBILITY:** Review confirmed banner/main/contentinfo landmarks; one h1 followed by section h2s and nested h3s; a focusable same-page playground target; native labeled controls with inclusive range/unit semantics; automatic WAI-ARIA tabs; polite copy/reset live regions; stable retry focus; 44px action targets; visible focus rules; and text/icon or border cues in addition to color. Review found two adoption-guidance blockers: the static horizontally scrollable import was not keyboard focusable, and the no-internal/CSS-subpath rule was implicit. A new RED in `App.test.tsx` failed 1/7, then passed 7/7 after adding `tabIndex=0` and explicit “Do not import internal modules or CSS subpaths.” guidance.
- **CONTRAST / CONTAINMENT:** Mathematical sRGB token checks (not rendered-browser claims) yielded text/surface 17.15:1, muted/surface 10.73:1, accent/control 13.41:1, accent-ink/accent 13.84:1, danger/surface 8.97:1, success/surface 11.66:1, and warning/surface 12.85:1. Preview uses layout/paint containment and clipping; generated/static code alone owns horizontal scrolling; grid children use `min-width: 0`; the body does not hide overflow.
- **FINAL AUTOMATED VERIFY (STATUS SUPERSEDED):** `pnpm test` passed Vitest 4.1.10 with 9 files/137 tests. `pnpm build` passed `tsc -b` and Vite 8.1.5 (30 modules; CSS 10.83 kB/3.28 kB gzip; JS 215.81 kB/67.94 kB gzip). `pnpm lint` passed with zero warnings/errors. These commands established automated behavior and buildability only. The earlier conclusion that checkpoint C was blocker-free, and the resulting 9.2/9.3 checkmarks, overreached because no executable browser was available for the required 1440/1024/768/390 inspection; a fresh checkpoint reopened 9.1–9.3.
- **REPOSITORY STATE:** `git status --short` exited 128 because this directory has no `.git` metadata. Consequently there is no staging area and no staged file can exist here, but a Git diff cannot be produced from this directory.

## Fresh checkpoint C remediation and status correction

- **REOPEN:** Tasks 9.1, 9.2, and 9.3 were reopened before remediation. Task 10 was not started.
- **RED:** Added one App composition case requiring exact `PACKAGE_URL`/`REPOSITORY_URL` links in both banner and contentinfo, with every npm/repository resource link carrying the dedicated target hook. Extended the CSS-source contract to require a 44px resource-link target and a `prefers-reduced-motion: reduce` smooth-scroll fallback. `pnpm exec vitest run src/App.test.tsx src/showcase/components/Playground.test.tsx` failed 2/26 as intended: hero/footer resources and both CSS rules were absent.
- **GREEN:** Added exact centralized npm/repository links to the inline hero and footer, retained the InstallGuide links, and applied `resource-link` to all six adoption links. `.resource-link` is an inline-flex target with `min-height: 44px`. `@media (prefers-reduced-motion: reduce)` now sets only showcase document `scroll-behavior: auto`; it does not override package animation. The focused command passed 2 files/26 tests.
- **TRIANGULATE / REFACTOR VERIFY:** Hero, InstallGuide, and footer links are scoped and checked independently against exact constants; InstallGuide keyboard/copy behavior remains covered. `pnpm exec vitest run src/App.test.tsx src/showcase/components/Playground.test.tsx src/showcase/components/InstallGuide.test.tsx` passed 3 files/35 tests. `pnpm test` passed Vitest 4.1.10 with 9 files/138 tests. `pnpm build` passed `tsc -b` and Vite 8.1.5 (30 modules; CSS 11.07 kB/3.34 kB gzip; JS 216.36 kB/67.99 kB gzip). `pnpm lint` passed with zero warnings/errors.
- **EXACT BROWSER ENVIRONMENT SEARCHES — NO EXECUTABLE FOUND:** `where.exe` searched `chrome.exe`, `msedge.exe`, `firefox.exe`, and `chromium.exe`; each returned “Could not find files for the given pattern(s).” PowerShell `Test-Path` returned `False` for Chrome, Edge, and Firefox under both Program Files roots and the current user's LocalAppData. `pnpm list playwright @playwright/test puppeteer --depth 0` returned no packages. `find node_modules` returned no Chrome/Edge/Firefox/Chromium executable. PowerShell `Test-Path` returned `False` for `C:\Users\anton\AppData\Local\ms-playwright`, `C:\Users\anton\.cache\ms-playwright`, and the duplicate LocalAppData browser-cache path. Project dependency listing confirmed jsdom/Vitest only, with no browser automation runtime.
- **EVIDENCE BOUNDARY:** jsdom tests establish semantics, state hooks, exact links, and focus/source order. CSS-source assertions establish declared selectors, breakpoints, containment, target sizes, reduced-motion scroll fallback, and absence of hidden-overflow/animation overrides. They do **not** establish rendered layout, computed target geometry, actual focus appearance, actual contrast, document overflow, package animation, or behavior at 1440/1024/768/390 CSS pixels. No browser/manual visual success is claimed.
- **TASK STATUS:** Task 9.1 is complete and re-checked after automated validation. Tasks 9.2 and 9.3 remain unchecked because their required rendered browser inspection/checkpoint cannot run in this environment. Task 10 remains untouched and unchecked.
