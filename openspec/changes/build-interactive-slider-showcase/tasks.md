# Tasks: Build Interactive Slider Showcase

## Execution Rules

- Use one writer for the entire change. Review checkpoints are read-only and must not introduce parallel writers.
- Keep all implementation inside the current Vite showcase repository. Do not edit the sibling `react-tech-slider` package, link local package source, or modify the old demo.
- Keep each test beside the production behavior it validates, using the file pairs listed below.
- Record every strict-TDD phase in `openspec/changes/build-interactive-slider-showcase/apply-progress.md` with: task ID, RED/GREEN/TRIANGULATE/REFACTOR label, exact command, relevant test names, intended failure or pass summary, and changed files. A final green run without prior RED evidence is insufficient.
- The only pre-RED production changes allowed are the published runtime dependency and the tooling required to execute Vitest. Do not create or restore a sentinel test.
- Use a validated text color field plus a non-interactive swatch. Do not use a native color picker.
- Build one canonical discriminated invocation in `model.ts`. Preview props and generated TSX must both consume that invocation; do not maintain independent preview/code prop mappings.
- Imports and generated examples must use only the published `react-tech-slider` package root. Never import a CSS subpath or package internals.

## 1. Published Runtime and Proportionate Test Harness

- [x] **1.1 Install and configure the approved dependency surface.** Add `react-tech-slider@^1.9.3` as a runtime dependency. Add compatible `vitest`, `jsdom`, `@testing-library/react`, `@testing-library/user-event`, and `@testing-library/jest-dom` development dependencies. Add `test: "vitest run"` and `test:watch: "vitest"`; configure jsdom, explicit Vitest imports, Testing Library cleanup, jest-dom, and only minimal deterministic `matchMedia` support. Do not add Playwright, Cypress, Storybook, axe wrappers, a CSS/UI framework, an icon package, or a sentinel test. Verify the installed package resolves from the registry and exposes its package-root declarations without editing package source. **Dependencies:** approved proposal/design only. **Expected files:** `package.json`, `pnpm-lock.yaml`, `vitest.config.ts`, `src/test/setup.ts`, `openspec/changes/build-interactive-slider-showcase/apply-progress.md`. **Commands:** `pnpm add react-tech-slider@^1.9.3`; `pnpm add -D vitest jsdom @testing-library/react @testing-library/user-event @testing-library/jest-dom`; `pnpm exec vitest --version`; `pnpm exec tsc --noEmit`. **Evidence:** record installed/resolved versions, package-root type resolution, script/config review, and command outcomes. Stop if the published release cannot install, its root exports do not match the approved API, or its peer requirements are incompatible with the scaffold's React 19 setup.

## 2. Canonical State, Normalization, and Invocation

- [x] **2.1 Implement canonical playground state and the single invocation projection with strict TDD.** Cover documented initial values, reduced-motion initialization of running playback only, field-specific reducer actions, finite numeric normalization for every specified domain, injectable CSS-color validation, separate shared/running/fades stores, validated dataset selection, exhaustive running/fades invocation entries, and atomic reset. Preview-only, install, copy, fallback, and retry state must not enter the invocation. **Dependencies:** 1.1. **Expected files:** `src/showcase/model.test.ts`, `src/showcase/model.ts`, `openspec/changes/build-interactive-slider-showcase/apply-progress.md`.
  - **RED:** write the first real running-invocation assertion before `model.ts` exists; run `pnpm exec vitest run src/showcase/model.test.ts` and record the intended missing-module/missing-behavior failure.
  - **GREEN:** implement the minimum typed state, defaults, normalization, active-config selection, and running projection needed for that assertion; rerun `pnpm exec vitest run src/showcase/model.test.ts` and record the passing test.
  - **TRIANGULATE:** add table-driven min/max/step, empty, NaN, Infinity, below/above-range cases for `iconWidth`, `borderWidth`, `durationMs`, `gap`, and `speed`; valid alpha/non-hex and invalid color cases; fades discriminant/allowed keys; forbidden running `speed`, `autoPlay`, and `pauseOnHover`; variant round trips; shared preservation; reduced-motion defaults; and full reset. Run `pnpm exec vitest run src/showcase/model.test.ts`, capture any new intended RED, generalize, and rerun to GREEN.
  - **REFACTOR:** remove duplication while preserving the discriminated types and one prop-selection authority; run `pnpm exec vitest run src/showcase/model.test.ts`, `pnpm test`, `pnpm build`, and `pnpm lint`. Record all four outcomes and the exact invocation-key parity assertions in apply-progress.

## 3. Code Generation and Generated-TSX Compilation

- [x] **3.1 Generate minimal copy-ready TypeScript/JSX only from the canonical invocation.** Emit a package-root import, minimal valid `Brand[]`, numeric JSX expressions, and a `Slider` expression. Running may omit its discriminator and exact package defaults; fades must emit `variant="fades"`, may omit `speed={1}`, and must retain controlled `gap={48}` and `iconWidth={5}` where omission changes the result. Never emit viewport/reset/install/copy/fallback state, CSS subpath imports, unsupported callbacks, `autoPlay`, `pauseOnHover`, or running `speed`. Compile representative generated strings with the TypeScript compiler API against the installed package declarations. **Dependencies:** 2.1. **Expected files:** `src/showcase/codegen.test.ts`, `src/showcase/codegen.ts`, `src/showcase/model.test.ts`, `openspec/changes/build-interactive-slider-showcase/apply-progress.md`.
  - **RED:** add an exact running-snippet test and an entry-parity assertion proving code generation consumes the model invocation; run `pnpm exec vitest run src/showcase/codegen.test.ts src/showcase/model.test.ts` and record the intended missing-generator failure.
  - **GREEN:** implement the smallest invocation-to-code-model renderer and source formatter; rerun the same focused command and record passing exact-string/parity tests.
  - **TRIANGULATE:** add fades output, non-default running values, numeric-not-string assertions, canonical brand serialization, omission rules, forbidden-name/root-import assertions, and TypeScript compiler-API checks for representative running and fades TSX. Run `pnpm exec vitest run src/showcase/codegen.test.ts`, capture any intended RED, generalize, and rerun to GREEN.
  - **REFACTOR:** keep prop selection out of `codegen.ts` and keep transport fallback URLs out of source; run `pnpm exec vitest run src/showcase/codegen.test.ts src/showcase/model.test.ts`, `pnpm test`, `pnpm build`, and `pnpm lint`. Record compiled fixture names, diagnostics count, and command outcomes in apply-progress.

- [x] **3.2 Complete internal review checkpoint A: model/source contract.** Review the canonical invocation, exact public prop names, normalization boundaries, source omissions, generated-TSX diagnostics, and absence of an independent preview/code mapping before starting browser-dependent work. **Dependencies:** 3.1. **Expected files reviewed, with no production edits in this task:** `src/showcase/model.ts`, `src/showcase/model.test.ts`, `src/showcase/codegen.ts`, `src/showcase/codegen.test.ts`, `package.json`, `pnpm-lock.yaml`, `openspec/changes/build-interactive-slider-showcase/apply-progress.md`. **Checkpoint outcome:** record reliability/readability findings and resolve blockers through a new RED-led change in the owning slice.

## 4. Curated Fixtures, Validation, and Stable Fallback URLs

- [x] **4.1 Add bounded canonical brand fixtures, integrity validation, and deterministic readable fallback data URLs with strict TDD.** Provide two curated datasets capped at six recognizable HTTPS logos each. Require finite unique IDs and non-empty trimmed names, distinguish empty from invalid data, preserve identity/order/dimensions, and generate stable SVG data URLs containing readable brand initials/name. Invalid or empty data must never be projected as a valid `Slider` invocation. **Dependencies:** 2.1. **Expected files:** `src/showcase/brands.test.ts`, `src/showcase/brands.ts`, `src/showcase/model.test.ts`, `openspec/changes/build-interactive-slider-showcase/apply-progress.md`.
  - **RED:** add a valid-fixture/unique-ID assertion and deterministic-fallback assertion; run `pnpm exec vitest run src/showcase/brands.test.ts` and record the intended missing-module/missing-behavior failure.
  - **GREEN:** implement the minimum fixtures, validator, and fallback encoder; rerun the focused command and record GREEN.
  - **TRIANGULATE:** add empty data, duplicate/non-finite IDs, blank names, non-HTTPS/empty fixture URLs, multiple brands, stable identity/order/dimensions, readable fallback content, and six-item cap cases. Add model tests proving empty/invalid results bypass ready invocation while valid data remains canonical. Run `pnpm exec vitest run src/showcase/brands.test.ts src/showcase/model.test.ts`, capture any intended RED, generalize, and rerun to GREEN.
  - **REFACTOR:** cache or centralize deterministic fallback construction without hiding validation rules; run `pnpm exec vitest run src/showcase/brands.test.ts src/showcase/model.test.ts`, `pnpm test`, `pnpm build`, and `pnpm lint`. Record fixture counts, validation counterexamples, and command outcomes in apply-progress.

## 5. Image Preflight and Late Package-Image Failure

- [x] **5.1 Resolve remote logos before package rendering and terminally recover late image failures with strict TDD.** Initially materialize deterministic fallback URLs, preflight each distinct canonical URL once through browser `Image`, use the canonical URL only after load/decode success, mark failure terminal without timers or automatic retries, clean stale callbacks by generation, and expose a late-failure reporter that replaces only a matching canonical-ready URL. Never retry a fallback URL or copy resolved/fallback URLs into generated guidance. **Dependencies:** 4.1. **Expected files:** `src/showcase/useResolvedBrands.test.tsx`, `src/showcase/useResolvedBrands.ts`, `src/showcase/brands.ts`, `openspec/changes/build-interactive-slider-showcase/apply-progress.md`.
  - **RED:** install a per-test fake `Image` and assert pending brands use fallbacks before one successful preflight; run `pnpm exec vitest run src/showcase/useResolvedBrands.test.tsx` and record the intended missing-hook failure.
  - **GREEN:** implement the minimum pending/success resolver; rerun the focused command and record GREEN.
  - **TRIANGULATE:** add one failure, all failures, decode rejection, duplicate URL deduplication, stale dataset cleanup, no retry after terminal failure, identity/order preservation, a post-preflight captured canonical URL failure, unrelated/fallback URL ignores, and proof that canonical code data stays unchanged. Run `pnpm exec vitest run src/showcase/useResolvedBrands.test.tsx`, capture intended RED cases, generalize, and rerun to GREEN.
  - **REFACTOR:** keep browser transport state outside the playground model and avoid fetch/canvas/blob/proxy workarounds; run `pnpm exec vitest run src/showcase/useResolvedBrands.test.tsx src/showcase/brands.test.ts`, `pnpm test`, `pnpm build`, and `pnpm lint`. Record image event sequences, retry counts, stale-callback result, and command outcomes in apply-progress.

## 6. Copy Feedback and Install Tabs

- [x] **6.1 Implement reusable clipboard feedback without hiding source, using strict TDD.** `CopyButton` must copy only its supplied exact string, handle absent clipboard APIs, synchronous failure, and rejected promises, retain visible/selectable source, and expose visible `aria-live="polite"` success or manual-copy failure feedback. Each instance owns independent transient state and cleans any timeout on unmount. **Dependencies:** 5.1. **Expected files:** `src/showcase/components/CopyButton.test.tsx`, `src/showcase/components/CopyButton.tsx`, `openspec/changes/build-interactive-slider-showcase/apply-progress.md`.
  - **RED:** assert the supplied text is written and a success announcement appears; run `pnpm exec vitest run src/showcase/components/CopyButton.test.tsx` and record the intended missing-component failure.
  - **GREEN:** implement the minimum activation/write/success state; rerun the focused command and record GREEN.
  - **TRIANGULATE:** add rejected, absent, and synchronous failure cases; no false success; retained source; independent instances; keyboard/native activation; and unmount cleanup. Run `pnpm exec vitest run src/showcase/components/CopyButton.test.tsx`, capture intended RED cases, generalize, and rerun to GREEN.
  - **REFACTOR:** retain the local discriminated copy state and avoid clipboard state in the canonical invocation; run `pnpm exec vitest run src/showcase/components/CopyButton.test.tsx`, `pnpm test`, `pnpm build`, and `pnpm lint`. Record clipboard mock outcome and announcements in apply-progress.

- [x] **6.2 Implement exact npm/pnpm guidance and WAI-ARIA tabs with strict TDD.** Centralize package name, npm/repository URLs, preset widths, and exact commands. Install selection defaults to npm and remains independently owned so playground reset cannot change it. Tabs must support click, native Enter/Space, Arrow Left/Right wrapping, Home, End, roving `tabIndex`, automatic selection, and a labeled active tabpanel. **Dependencies:** 6.1. **Expected files:** `src/showcase/constants.ts`, `src/showcase/components/InstallGuide.test.tsx`, `src/showcase/components/InstallGuide.tsx`, `src/showcase/components/CopyButton.tsx`, `openspec/changes/build-interactive-slider-showcase/apply-progress.md`.
  - **RED:** assert the default tab shows exactly `npm install react-tech-slider` and copies that exact command; run `pnpm exec vitest run src/showcase/components/InstallGuide.test.tsx` and record the intended missing-component failure.
  - **GREEN:** implement the minimal npm/pnpm tab state, panel, and copy integration; rerun the focused command and record GREEN.
  - **TRIANGULATE:** add exact `pnpm add react-tech-slider`, click switching, Arrow wrapping, Home/End, roving focus, native activation, associated tabpanel semantics, non-color selection text/icon, and clipboard rejection. Run `pnpm exec vitest run src/showcase/components/InstallGuide.test.tsx src/showcase/components/CopyButton.test.tsx`, capture intended RED cases, generalize, and rerun to GREEN.
  - **REFACTOR:** keep command grammar as exact constants and install state outside playground reset; run the focused files, `pnpm test`, `pnpm build`, and `pnpm lint`. Record copied strings, keyboard paths, and command outcomes in apply-progress.

## 7. Controls, State Preservation, Reset, and Generated Source

- [x] **7.1 Implement variant-aware native controls and playground coordination with strict TDD.** `Playground` is the sole package-setting state owner. `ControlPanel` conditionally mounts only active-variant controls, uses labels/fieldsets/units/native semantics, retains ephemeral invalid drafts locally, commits only normalized values, returns focus predictably to the selected variant radio when a control subtree disappears, and keeps the reset button focused after atomic reset. Shared and variant edits must survive round trips; reset restores the captured initial variant, both stores, dataset, and viewport but not install selection. The generated source must update from the same canonical invocation as preview props. **Dependencies:** 3.2, 4.1, 6.2. **Expected files:** `src/showcase/components/Playground.test.tsx`, `src/showcase/components/Playground.tsx`, `src/showcase/components/ControlPanel.tsx`, `src/showcase/components/GeneratedCode.tsx`, `src/showcase/model.ts`, `src/showcase/model.test.ts`, `openspec/changes/build-interactive-slider-showcase/apply-progress.md`.
  - **RED:** mock only the downstream preview boundary and assert default running controls plus synchronized generated source; run `pnpm exec vitest run src/showcase/components/Playground.test.tsx` and record the intended missing-component failure.
  - **GREEN:** implement minimum state ownership, active controls, dispatch wiring, and generated-code rendering; rerun the focused command and record GREEN.
  - **TRIANGULATE:** add fades-only controls and exact `variant="fades"` source, absence/non-operability of inactive controls, every unit/range, invalid numeric/color drafts, shared/variant round-trip preservation, viewport source exclusion, reduced-motion initial pause, reset of both stores/dataset/viewport, install-tab persistence in an integration case, focus after variant switch/reset, running play mapping to `isPlay`, and the exact fades no-pause/full-reduced-motion limitation note. Run `pnpm exec vitest run src/showcase/components/Playground.test.tsx src/showcase/model.test.ts src/showcase/codegen.test.ts`, capture intended RED cases, generalize, and rerun to GREEN.
  - **REFACTOR:** keep field logic out of `App`, prop construction out of `ControlPanel`, and source interpretation out of `GeneratedCode`; run the three focused files, `pnpm test`, `pnpm build`, and `pnpm lint`. Record control names, focus targets, parity assertions, and command outcomes in apply-progress.

## 8. Published Preview, Data States, Error Boundary, and Recovery

- [x] **8.1 Render only validated, resolved published-package props and contain data/image failures with strict TDD.** `PreviewPanel` must derive `SliderProps` by iterating the canonical invocation entries and substituting only resolved brand URLs. It must render the package-root `Slider` for ready data, omit `variant` for running if chosen by the projection, add `variant="fades"` for fades, and never select prop names independently. Empty/invalid data must bypass `Slider` and retain canonical integration guidance. An `onErrorCapture` handler must report late failures only for package images matching canonical ready URLs. Preset controls change only canvas metadata/width and never browser globals or generated props. **Dependencies:** 5.1, 7.1. **Expected files:** `src/showcase/components/Playground.tsx`, `src/showcase/components/Playground.test.tsx`, `src/showcase/components/PreviewPanel.tsx`, `src/showcase/model.ts`, `src/showcase/model.test.ts`, `openspec/changes/build-interactive-slider-showcase/apply-progress.md`.
  - **RED:** mock the package-root `Slider` and assert exact default running preview props equal the canonical invocation entries plus resolved `brandsList`; run `pnpm exec vitest run src/showcase/components/Playground.test.tsx` and record the intended missing-preview failure.
  - **GREEN:** implement minimal ready rendering and invocation-to-preview materialization; rerun the focused command and record GREEN.
  - **TRIANGULATE:** add exact fades props, forbidden-key absence, viewport/reset/copy/install exclusion, empty and invalid data bypass, readable local messages/guidance, desktop/tablet/mobile `aria-pressed` and width metadata, generated-source invariance under viewport changes, late matching image failure, unrelated/fallback image ignores, and all-fallback usability. Run `pnpm exec vitest run src/showcase/components/Playground.test.tsx src/showcase/useResolvedBrands.test.tsx`, capture intended RED cases, generalize, and rerun to GREEN.
  - **REFACTOR:** preserve one invocation mapping and keep transport substitution as the only preview-specific transform; run the two focused files plus `src/showcase/model.test.ts`, `pnpm test`, `pnpm build`, and `pnpm lint`. Record exact mock calls, bypass cases, preset invariance, and command outcomes in apply-progress.

- [x] **8.2 Contain package rendering failures and provide explicit retry/reset recovery with strict TDD.** A class error boundary must wrap only the package render subtree. Its fallback must be an in-canvas `role="alert"` with “Live preview unavailable,” Retry preview, and Reset settings and retry. Only the dedicated retry key remounts the boundary; ordinary settings, viewport, copy, or install changes must not. Recovery focuses a stable preview heading/status outside the keyed subtree. Controls, source, install guidance, links, and fades disclosure must remain mounted, and repeated failure must remain contained. **Dependencies:** 8.1. **Expected files:** `src/showcase/components/PreviewErrorBoundary.test.tsx`, `src/showcase/components/PreviewErrorBoundary.tsx`, `src/showcase/components/PreviewPanel.tsx`, `src/showcase/components/Playground.test.tsx`, `src/showcase/components/Playground.tsx`, `openspec/changes/build-interactive-slider-showcase/apply-progress.md`.
  - **RED:** render a throwing child and assert a local alert rather than an uncaught page failure; run `pnpm exec vitest run src/showcase/components/PreviewErrorBoundary.test.tsx` and record the intended uncaught/missing-boundary failure while scoping expected console suppression to this test.
  - **GREEN:** implement the minimum class boundary and local fallback; rerun the focused command and record GREEN.
  - **TRIANGULATE:** add retry remount with current normalized state, reset-and-retry with captured initial state, stable focus target, repeated throw containment, no remount for ordinary changes, and an integration assertion that controls/source/install/disclosure remain reachable during failure. Run `pnpm exec vitest run src/showcase/components/PreviewErrorBoundary.test.tsx src/showcase/components/Playground.test.tsx`, capture intended RED cases, generalize, and rerun to GREEN.
  - **REFACTOR:** keep the boundary narrow and retry state out of package/source models; run both focused files, `pnpm test`, `pnpm build`, and `pnpm lint`. Record fallback scope, remount counts, focus result, and command outcomes in apply-progress.

- [x] **8.3 Complete internal review checkpoint B: failure resilience and interactions.** Review remote preflight/late failure, no-retry guarantees, clipboard denial, empty/invalid data, package error containment, recovery focus, exact controls, state/source parity, and fades disclosure. **Dependencies:** 8.2. **Expected files reviewed, with no production edits in this task:** `src/showcase/brands.ts`, `src/showcase/useResolvedBrands.ts`, `src/showcase/components/CopyButton.tsx`, `src/showcase/components/InstallGuide.tsx`, `src/showcase/components/ControlPanel.tsx`, `src/showcase/components/Playground.tsx`, `src/showcase/components/PreviewPanel.tsx`, `src/showcase/components/PreviewErrorBoundary.tsx`, their colocated tests, `openspec/changes/build-interactive-slider-showcase/apply-progress.md`. **Checkpoint outcome:** record reliability/resilience findings; resolve any blocker with a new RED in its owning slice before page styling.

## 9. Single-Page Composition, CSS, and Accessibility

- [x] **9.1 Compose the complete one-page evaluation/adoption flow with strict TDD.** Replace the Vite placeholder with ordered landmarks and headings for hero, in-page playground target, visually primary playground, install guide, concise package/variant/API guidance, and footer. Use distinct npm and repository links, consistent package naming, public-root import guidance, and repeated honest fades disclosure. Keep preview source before controls in DOM order for narrow layouts and avoid routing or extracted one-use component abstractions. **Dependencies:** 8.3. **Expected files:** `src/App.test.tsx`, `src/App.tsx`, `src/main.tsx`, `openspec/changes/build-interactive-slider-showcase/apply-progress.md`.
  - **RED:** assert the hero, same-page playground target, playground landmark/heading, install area, API guidance, distinct package/repository links, and fades limitation; run `pnpm exec vitest run src/App.test.tsx` and record the intended placeholder-content failure.
  - **GREEN:** implement the minimum complete section composition and wire existing components; rerun the focused command and record GREEN.
  - **TRIANGULATE:** add ordered heading/landmark assertions, focusable in-page target, consistent package name/URLs, public-root/no-CSS-subpath guidance, preview-before-controls source order, persistent guidance during a mocked preview failure, and absence of route/account/persistence/editor claims. Run `pnpm exec vitest run src/App.test.tsx src/showcase/components/Playground.test.tsx`, capture intended RED cases, generalize, and rerun to GREEN.
  - **REFACTOR:** keep `App.tsx` composition-only and retain static one-use sections inline; run both focused files, `pnpm test`, `pnpm build`, and `pnpm lint`. Record landmark/link queries and command outcomes in apply-progress.

- [ ] **9.2 Implement the dark editorial, responsive, high-contrast CSS system with testable hooks and strict evidence.** Add bounded page sections, restrained decorative gradients, wide two-column and narrow one-column playground layouts, preview dominance, `min-width: 0`, wrapped tab/preset groups, 44px primary targets, visible `:focus-visible`, non-color selected/invalid/success/paused cues, bounded code overflow, and preview canvas containment at 960/768/390px. Do not rely on `body { overflow-x: hidden }` to hide layout defects and do not override package animation to imply reduced-motion support. Automated tests may verify semantic state hooks/classes/data attributes, but real layout, contrast, focus visibility, and animation remain manual claims. **Dependencies:** 9.1. **Expected files:** `src/styles.css`, `src/main.tsx`, `src/App.test.tsx`, `src/showcase/components/Playground.test.tsx`, `openspec/changes/build-interactive-slider-showcase/apply-progress.md`.
  - **RED:** add assertions for the stable classes/data attributes required by preview width, selected/non-color state, bounded code region, alerts/notes, and focus targets; run `pnpm exec vitest run src/App.test.tsx src/showcase/components/Playground.test.tsx` and record the intended missing-style-hook failures. Do not claim jsdom proves pixels or contrast.
  - **GREEN:** implement the minimum tokens, responsive grids, preview/code containment, native-control styling, state cues, and focus rules to satisfy the structural contract; rerun the focused command and record GREEN.
  - **TRIANGULATE:** add narrow/wide structural cases, long source, wrapped groups, all preset attributes, invalid/success/paused cues, and package failure/empty/fallback states. Run the focused tests, then inspect at 1440, 1024/768, and 390 CSS pixels; capture any intended automated RED or manual blocker, correct it, and rerun/reinspect.
  - **REFACTOR:** consolidate tokens and repeated selectors without creating a component/CSS framework; run `pnpm exec vitest run src/App.test.tsx src/showcase/components/Playground.test.tsx`, `pnpm test`, `pnpm build`, and `pnpm lint`. Record automated outcomes and explicitly label manual CSS observations separately from jsdom evidence.

- [ ] **9.3 Complete internal review checkpoint C: page, CSS, and accessibility.** Review source/focus order, landmarks, labels, units, range semantics, tab semantics, live regions, limitation note, non-color cues, CSS containment, long-code scrolling, dependency scope, and lack of unnecessary abstractions. **Dependencies:** 9.2. **Expected files reviewed, with no production edits in this task:** `src/App.tsx`, `src/App.test.tsx`, `src/main.tsx`, `src/styles.css`, `src/showcase/components/Playground.tsx`, `src/showcase/components/Playground.test.tsx`, `src/showcase/components/ControlPanel.tsx`, `src/showcase/components/PreviewPanel.tsx`, `src/showcase/components/PreviewErrorBoundary.tsx`, `src/showcase/components/PreviewErrorBoundary.test.tsx`, `src/showcase/components/GeneratedCode.tsx`, `src/showcase/components/InstallGuide.tsx`, `src/showcase/components/InstallGuide.test.tsx`, `src/showcase/components/CopyButton.tsx`, `src/showcase/components/CopyButton.test.tsx`, `package.json`, `pnpm-lock.yaml`, `openspec/changes/build-interactive-slider-showcase/apply-progress.md`. **Checkpoint outcome:** record readability/reliability/accessibility findings and resolve blockers with RED-led changes in the owning task.

## 10. Delivery and Reviewer-Workload Forecast

- [ ] **10.1 Reassess delivery after all internal checkpoints.** **Dependencies:** 9.3. **Expected files:** `openspec/changes/build-interactive-slider-showcase/apply-progress.md` only. The default single PR remains coherent because the graph delivers one page, one published dependency, one canonical invocation pipeline, colocated tests, and failure handling that is inseparable from the preview it protects. Reviewer workload is **substantial but cohesive**: pure model/codegen tests reduce behavioral ambiguity; remote/clipboard/error paths carry medium resilience risk; the lockfile will be noisy but mechanically isolated to the approved runtime/test dependencies; the test volume is high but organized beside behavior; and the single stylesheet may be visually dense but remains one bounded design system. Keep one PR with checkpoint A (model/codegen), checkpoint B (failures/interactions), checkpoint C (page/CSS/accessibility), and final verification as explicit internal review boundaries. Recommend a delivery change only if actual implementation breaks these boundaries—for example, unrelated dependencies, package-source edits, multiple competing state/projection paths, an unreviewably entangled stylesheet, or unresolved failures spanning several checkpoints. Do not split merely because the lockfile or test count is large, and do not use a fixed changed-line threshold.

## 11. Non-Goals and Stop Conditions

- [ ] **11.1 Confirm non-goals before final verification.** **Dependencies:** 10.1. **Expected files reviewed:** `package.json`, `pnpm-lock.yaml`, `vitest.config.ts`, `src/test/setup.ts`, `src/App.tsx`, `src/App.test.tsx`, `src/main.tsx`, `src/styles.css`, `src/showcase/constants.ts`, `src/showcase/model.ts`, `src/showcase/model.test.ts`, `src/showcase/codegen.ts`, `src/showcase/codegen.test.ts`, `src/showcase/brands.ts`, `src/showcase/brands.test.ts`, `src/showcase/useResolvedBrands.ts`, `src/showcase/useResolvedBrands.test.tsx`, `src/showcase/components/Playground.tsx`, `src/showcase/components/Playground.test.tsx`, `src/showcase/components/ControlPanel.tsx`, `src/showcase/components/PreviewPanel.tsx`, `src/showcase/components/PreviewErrorBoundary.tsx`, `src/showcase/components/PreviewErrorBoundary.test.tsx`, `src/showcase/components/GeneratedCode.tsx`, `src/showcase/components/InstallGuide.tsx`, `src/showcase/components/InstallGuide.test.tsx`, `src/showcase/components/CopyButton.tsx`, `src/showcase/components/CopyButton.test.tsx`; **expected write:** `openspec/changes/build-interactive-slider-showcase/apply-progress.md` only. Confirm there is no package rebuild/patch, local package link, old-demo edit, custom animation, new public prop/callback, fades pause control, full reduced-motion claim, route, account, persistence, analytics, backend/proxy, service worker, arbitrary dataset editor, asset upload, code sandbox, carousel, CSS/UI framework, icon package, internal/CSS-subpath import, browser-global viewport emulation, native color picker, sentinel test, or independent preview/code prop mapping.

- [ ] **11.2 Enforce stop conditions rather than improvising.** **Dependencies:** 11.1. **Expected file:** `openspec/changes/build-interactive-slider-showcase/apply-progress.md` only; make no implementation write until the blocker is resolved. Stop and return for a decision if: the published package/root declarations are incompatible; React peer requirements cannot be satisfied without package changes; strict RED fails for tooling rather than intended behavior after bootstrap; generated representative TSX cannot compile against installed declarations; a required behavior needs a package API not present in 1.9.3-compatible releases; remote fallback would require a backend/proxy or package patch; implementation would touch the sibling package or old demo; a second writer becomes necessary; scope expands into a non-goal; internal review finds state/source drift or an uncontained failure; manual browser checks expose unresolved overflow, keyboard, focus, contrast, clipboard, remote-image, or recovery blockers; or the qualitative review forecast shows the task graph is no longer cohesive as one PR.

## 12. Final Automated and Manual Verification

- [ ] **12.1 Run the complete automated test suite:** `pnpm test`. **Dependencies:** 11.2. **Expected file:** `openspec/changes/build-interactive-slider-showcase/apply-progress.md` only. Record exit code, Vitest version, test-file/test counts, and any expected console suppression.
- [ ] **12.2 Run the production build:** `pnpm build`. **Dependencies:** 12.1. **Expected file:** `openspec/changes/build-interactive-slider-showcase/apply-progress.md` only. Record exit code and concise TypeScript/Vite output; treat any generated-TSX or package-public-prop diagnostic as a blocker.
- [ ] **12.3 Run lint:** `pnpm lint`. **Dependencies:** 12.2. **Expected file:** `openspec/changes/build-interactive-slider-showcase/apply-progress.md` only. Record exit code and warning/error count; do not accept new ignored failures.
- [ ] **12.4 Run and document the manual browser matrix.** **Dependencies:** 12.3. **Expected file:** `openspec/changes/build-interactive-slider-showcase/apply-progress.md` only. Use current Chromium, Firefox, and WebKit/Safari where available; record any unavailable engine explicitly rather than claiming coverage. Exercise the wide matrix at approximately 1440 CSS px, the intermediate/tablet matrix at 1024 and 768 CSS px, and the mobile matrix at 390 CSS px. At every representative width verify navigation/reachability, preview prominence/containment, no document horizontal overflow, bounded code scrolling, visible focus, non-color state cues, and primary target sizing. Across the matrix record pass or blocker for: real published running and fades animation; every visible control and both inclusive boundaries; units and invalid numeric/color commits; variant round-trip state and full reset with install selection preserved; generated-code synchronization, root imports, compilation/copyability, and forbidden-prop absence; npm/pnpm mouse and keyboard tabs with exact copied commands; clipboard success/denial/absence; keyboard-only completion and focus after variant changes/recovery; desktop/tablet/mobile preview presets without browser-global or source changes; normal remote logos, one failed URL, broad/offline failure, stable readable fallbacks, and a simulated late post-preflight failure; unique IDs, invalid ID/name, and empty data through the development test seam; forced package render failure, retry, reset-and-retry, and repeated contained failure while guidance remains usable; measured text/control contrast with browser tooling; running pause and operating-system reduced-motion initialization; fades at speed/gap extremes and all presets; repeated package alt behavior; the exact visible fades no-pause/full-reduced-motion limitation; package/repository links; consistent package naming; and absence of routes, storage, analytics, backend traffic, arbitrary editing, package-source changes, native color picker, or preview/code mapping divergence.
