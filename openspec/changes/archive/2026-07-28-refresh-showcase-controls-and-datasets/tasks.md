# Implementation Tasks: Refresh Showcase Controls and Datasets

## 2026-07-28 Sport fixture contract amendment

The current production `SPORT_BRANDS` is authoritative: seven ordered IDs 1–7 for Puma, Reebok, Under Armour, The North Face, Nike, Adidas, and Fila, with current URLs/class names and both `transition: "width 1s ease"` and `filter: "invert()"` in every style. New Balance, Converse, and Asics are removed. Food and `MAX_DATASET_SIZE` remain 10; fades renders 49 Sport and 100 Food images. Original Unit A–H ten-record Sport evidence below is retained as historical true-at-the-time evidence and is superseded for current acceptance.

## Apply gate and delivery decision

Apply is blocked until the parent decision below is recorded. One writer owns all production and test edits. Tests, CSS, and `apply-progress.md` evidence stay in the same work unit as the behavior they verify. This tasks phase performs no implementation, dependency rewrite, commit, branch, or PR action.

- [x] **0.1 Record the mandatory delivery decision before any implementation task starts.** Record exactly one choice in `openspec/changes/refresh-showcase-controls-and-datasets/apply-progress.md`: **chained delivery** using Work Units A through H in order, or **one PR with an explicit `size:exception`**. Chained delivery is recommended because every forecast unit is below the 400-changed-line review budget and can remain independently green. The one-PR alternative keeps the same internal work-unit boundaries and evidence but requires the explicit `size:exception`; this tasks artifact does not choose it. **Command:** `git status --short`. **Acceptance evidence:** the dated decision, approver, selected strategy, one-writer constraint, and statement that no implementation task preceded the decision are recorded.

- [x] **0.2 Run and record apply preflight without rewriting user-owned dependency files.** Likely evidence file: `openspec/changes/refresh-showcase-controls-and-datasets/apply-progress.md`; read-only inputs: `package.json`, `pnpm-lock.yaml`, `src/App.tsx`. Acknowledge that `package.json` and `pnpm-lock.yaml` were already user-modified before apply and are protected from this change. **Commands:** `git status --short`; `git diff -- package.json pnpm-lock.yaml`; `git hash-object package.json pnpm-lock.yaml`; `node -e "const p=require('./package.json'); console.log(p.dependencies['react-tech-slider'], p.dependencies['react-colorful'])"`; `pnpm list react-tech-slider react-colorful --depth 0`; `pnpm test`; and the protected-hero hash command from task 10.4. **Acceptance evidence:** declared ranges remain `^1.10.1` and `^5.8.0`, installed resolutions are exactly `react-tech-slider@1.10.1` and `react-colorful@5.8.0`, baseline manifest/lock hashes and existing diffs are captured, current tests pass or a pre-existing failure is documented and escalated, and the current final approved hero hash equals `aa787e708db68498c3b051446d7416b9933b2455f238bcf49e346bee3b56bdee` with `iconWidth={18}`.

- [x] **0.3 Freeze scope and stop conditions before RED.** Allowed implementation paths are the files named in Work Units A–H, `src/styles.css`, `src/App.test.tsx`, and per-slice evidence in `openspec/changes/refresh-showcase-controls-and-datasets/apply-progress.md`. The exact user-approved `src/App.tsx` amendment is limited to importing `CORE_BRANDS`, removing the duplicated hero-local array, passing `brandsList={CORE_BRANDS}`, and setting the existing hero Slider to `iconWidth={18}`; after that amendment, `src/App.tsx`, dependencies, package source, and prior changes remain read-only. **Commands:** `git diff --name-only`; `git status --short src/App.tsx package.json pnpm-lock.yaml`. **Acceptance evidence:** apply progress records that work stops immediately for (a) any `src/App.tsx` diff beyond the approved amendment or final hero hash mismatch, (b) a proposed package patch/wrapper/private-selector or animation workaround, (c) dependency range/resolution or manifest/lock drift, (d) generated-code syntax or published-`Brand` type diagnostics, or (e) a browser blocker affecting required interaction, containment, alignment, or ten-item fades performance. Resolution requires parent/user direction; it must not be patched around silently.

- [x] **0.4 Record the final 2026-07-28 user-approved mid-apply hero scope amendment and rebaseline.** Preserve the current `src/App.tsx` diff exactly: import `CORE_BRANDS`, delete the duplicate hero-local brands array, pass `brandsList={CORE_BRANDS}`, and set the existing hero Slider to `iconWidth={18}`. Record historical hash `2b40e750e89c920657832665f59749b0d97cf7a0069b884b938d99fc71d46346`, superseded intermediate hash `8c04b455f6ea3fe26b12a0a6e16e2c003e7a86297e168bc3eba73bf6dd3c7ca6`, final accepted hash `aa787e708db68498c3b051446d7416b9933b2455f238bcf49e346bee3b56bdee`, explicit user approval, and the Unit G stop/resolution in every amended SDD artifact. **Commands:** the task 10.4 hash command; `git diff --check -- openspec/changes/refresh-showcase-controls-and-datasets`; `git diff --cached --name-only`. **Acceptance evidence:** the final hash matches, all six artifacts agree that the accepted hero width is 18 and package integration is unchanged, prior apply evidence remains clearly historical, no production edit occurs during the artifact-only resume, no file is staged, and H has not started.

## Reviewable chained sequence

| Order | Candidate work unit | Depends on | Forecast changed lines | Independent green boundary |
| ---: | --- | --- | ---: | --- |
| 1 | A — exact fixtures and max-ten validation | Gate 0 | 290–360 | Fixture/validator focused suite passes; legacy selector may remain temporarily |
| 2 | B — Core/Sport/Food dataset wiring | A | 130–180 | Selector/model/playground suites pass; Frontend is absent |
| 3 | C — metadata fallback and stable code serialization | B | 190–240 | Generated metadata compiles against public `Brand` |
| 4 | D — five live native ranges | C | 150–210 | All five controls have immediate preview/source parity |
| 5 | E — concrete CSS-color resolver | D | 180–230 | Pure resolver matrix passes without UI coupling |
| 6 | F — both alpha pickers and text fallbacks | E | 160–210 | Picker/text/preview/source synchronization passes |
| 7 | G — bounded tokenizer and generated-code viewer | F | 220–290 | Safe highlighted viewer and exact copy pass |
| 8 | H — reusable copy composition and inline install field | G | 130–180 | Both copy consumers and responsive field pass |

The aggregate remains the design forecast of approximately **1,450–1,900 changed lines**. No unit is forecast above 400. At each boundary, count additions plus deletions for that unit's files. If a unit reaches 400, stop before lifecycle actions and split by coherent behavior with its tests: A may split into Core/max-policy and Sport/Food-fixture units; G may split into pure tokenizer and viewer-composition units; any other unit requires a newly recorded split plan. Never split tests or user-facing CSS away from the behavior they verify.

## Work Unit A — exact fixtures and max-ten validation

**Likely files:** `src/showcase/brands.ts`, `src/showcase/brands.test.ts`, and slice evidence in `openspec/changes/refresh-showcase-controls-and-datasets/apply-progress.md`.

- [x] **A.RED — Specify the fixture and validation contract first.** Add failing tests for the exact six Core records with no fixture `width`/`height`, exact ordered ten-record `SPORT_BRANDS` and `FOOD_BRANDS` literals from the approved spec, every HTTPS URL, `style: { transition: "width 1s ease" }`, the six exact Sport class names, omitted class names elsewhere, and exact Food punctuation/case (`SubWay`, `StarBucks`, <code>Dunkin&#96;Donuts</code>, <code>Domino&#96;s Pizza</code>). Require `MAX_DATASET_SIZE === 10`, valid lengths 1 and 10, invalid lengths 0 and 11, duplicate rejection within one list, and serializable optional metadata validation. **Command:** `pnpm exec vitest run src/showcase/brands.test.ts`. **Acceptance evidence:** apply progress captures the intended failing assertions and confirms failures arise from missing fixtures, Core dimensions, the six-item cap, or missing metadata guards—not unrelated baseline failures.

- [x] **A.GREEN — Add the minimum exact fixture and validator implementation.** In `src/showcase/brands.ts`, retain Core identity/order/URLs while removing only its `width` and `height`; add the exact Sport/Food arrays; add named `MAX_DATASET_SIZE = 10`; accept 1–10 and reject 11; preserve array/record identity and order; validate optional finite dimensions, exact string `className`, and non-null/non-array `style` whose own enumerable values are strings or finite numbers. Keep the legacy Frontend export only until Work Unit B if current consumers require it to keep this unit green. **Command:** `pnpm exec vitest run src/showcase/brands.test.ts`. **Acceptance evidence:** focused tests pass and no fixture value differs from the normative appendix.

- [x] **A.TRIANGULATE — Prove boundary and preservation cases.** Test empty/one/ten/eleven records; repeated IDs across two separately validated arrays versus duplicates inside one array; absent/empty style; `style.width`; string and numeric CSS values; invalid NaN/infinite dimensions; invalid array/null/object-valued style entries; class-name case; fallback materialization changing only `img`; original-reference preservation; and canonical URL retention. **Command:** `pnpm exec vitest run src/showcase/brands.test.ts`. **Acceptance evidence:** all matrix cases pass, ten is the hard ceiling, and valid metadata is never cloned, stripped, normalized, or reordered.

- [x] **A.REFACTOR — Consolidate fixture/validation policy and close the unit.** Keep one validator and one fallback replacement rule; remove duplicated limit literals without changing behavior. **Commands:** `pnpm exec vitest run src/showcase/brands.test.ts`; `git diff --check`; `git diff --numstat -- src/showcase/brands.ts src/showcase/brands.test.ts`; and the protected-hero hash command from task 10.4. **Acceptance evidence:** the suite remains green, changed lines are below 400, the hero hash is unchanged, and `apply-progress.md` records RED → GREEN → TRIANGULATE → REFACTOR outputs plus the unit diff count. Stop and split A as described above if the count reaches 400.

## Work Unit B — exact dataset wiring and canonical selection

**Likely files:** `src/showcase/model.ts`, `src/showcase/model.test.ts`, `src/showcase/components/Playground.tsx`, `src/showcase/components/ControlPanel.tsx`, `src/showcase/components/Playground.test.tsx`, `src/showcase/brands.ts`, and slice evidence in `apply-progress.md`.

- [x] **B.RED — Specify exact selector and state behavior.** Add failing tests that `DatasetId` and the default map support only `core | sport | food`; the selector exposes exactly `Core`, `Sport`, `Food` in order and never Frontend; each selection passes the matching exact canonical array to the invocation; repeated IDs in Sport and Food are independently valid; reset returns to Core; and preview/source switch together. **Commands:** `pnpm exec vitest run src/showcase/model.test.ts src/showcase/components/Playground.test.tsx`. **Acceptance evidence:** failures identify the old Frontend union/map/option and missing Sport/Food selection.

- [x] **B.GREEN — Wire the approved datasets through the existing canonical pipeline.** Change `DatasetId`, the exhaustive `DEFAULT_DATASETS`, and selector options; remove Frontend fixture/import/option/tests; preserve `Playground -> projectSlider -> SliderInvocation -> preview/source` as the only flow. Do not add a second prop or source mapping. **Commands:** `pnpm exec vitest run src/showcase/brands.test.ts src/showcase/model.test.ts src/showcase/components/Playground.test.tsx`. **Acceptance evidence:** Core/Sport/Food selection is green, Frontend is absent, reset is Core, and preview/source use the same selected invocation.

- [x] **B.TRIANGULATE — Prove selection, reset, invalid-data containment, and icon-width inheritance.** Cover all three selections, running/fades variant isolation, reset, an injected invalid/duplicate list, Core records without size overrides, and Sport/Food metadata without `width`, `height`, or `style.width`. **Command:** `pnpm exec vitest run src/showcase/brands.test.ts src/showcase/model.test.ts src/showcase/components/Playground.test.tsx`. **Acceptance evidence:** valid datasets reach the package mock in exact order; invalid data remains contained; no fixture masks public `iconWidth`; no preview-preset value enters generated source.

- [x] **B.REFACTOR — Remove all Frontend residue and close the unit.** Search types, fixtures, options, tests, and labels while preserving unrelated use of the word “frontend” in prose if any. **Commands:** `rg -n 'FRONTEND_BRANDS|DatasetId.*frontend|value="frontend"|Frontend frameworks' src`; `pnpm exec vitest run src/showcase/brands.test.ts src/showcase/model.test.ts src/showcase/components/Playground.test.tsx`; `git diff --check`; `git diff --numstat -- src/showcase/brands.ts src/showcase/model.ts src/showcase/model.test.ts src/showcase/components/Playground.tsx src/showcase/components/ControlPanel.tsx src/showcase/components/Playground.test.tsx`; and the hero hash command. **Acceptance evidence:** the targeted search has no matches, tests pass, changed lines are below 400, and apply progress contains phase evidence and the unchanged hero hash.

## Work Unit C — metadata preservation and stable generated source

**Likely files:** `src/showcase/codegen.ts`, `src/showcase/codegen.test.ts`, `src/showcase/useResolvedBrands.test.tsx`, optionally `src/showcase/useResolvedBrands.ts` only if a failing preservation test proves a production fix is necessary, and slice evidence in `apply-progress.md`.

- [x] **C.RED — Specify stable serialization and transport preservation.** Add failing tests for field order `id`, `name`, `img`, optional `width`, optional `height`, optional `style`, optional `className`; JSON string escaping; identifier-safe versus quoted style keys; insertion-order style entries; empty versus absent style; exact backticks/punctuation; `style.width`; fallback pending/ready/failed states; and canonical HTTPS URLs never becoming fallback `data:` URLs in source. Add TypeScript compiler-API fixtures for Sport metadata, a Food backtick name, `style.width`, and absent metadata, assignable to the public package `Brand`. **Commands:** `pnpm exec vitest run src/showcase/codegen.test.ts src/showcase/useResolvedBrands.test.tsx`. **Acceptance evidence:** failures are specifically absent metadata serialization or transport assertions; existing generated code remains the canonical source input.

- [x] **C.GREEN — Implement the minimum stable serializer.** Extend `serializeBrand` in `src/showcase/codegen.ts` without changing invocation construction; preserve exact metadata and omit absent fields. Retain the existing `{ ...brand, img: replacement }` fallback path unless RED proves it deficient. **Commands:** `pnpm exec vitest run src/showcase/brands.test.ts src/showcase/codegen.test.ts src/showcase/useResolvedBrands.test.tsx`. **Acceptance evidence:** focused tests pass, generated records use stable field order and canonical URLs, and compiler diagnostics are empty.

- [x] **C.TRIANGULATE — Prove escaping and public-type correctness.** Cover quotes, backslashes, `<`, `&`, backticks, custom CSS keys, numeric values, finite dimensions, empty objects, optional combinations, record order/identity, all image states, and package-root imports only. **Command:** `pnpm exec vitest run src/showcase/codegen.test.ts src/showcase/useResolvedBrands.test.tsx`. **Acceptance evidence:** token-for-token literal values reconstruct canonical metadata, no fallback URL is emitted, and every representative generated file has zero syntax and public-`Brand` assignment errors. **Stop:** any generated-code diagnostic blocks the unit; do not weaken or skip compiler checks.

- [x] **C.REFACTOR — Keep one serializer and close the unit.** Remove duplicate metadata/source mapping and retain source-generation responsibility in `codegen.ts`. **Commands:** `pnpm exec vitest run src/showcase/brands.test.ts src/showcase/model.test.ts src/showcase/codegen.test.ts src/showcase/useResolvedBrands.test.tsx`; `git diff --check`; `git diff --numstat -- src/showcase/codegen.ts src/showcase/codegen.test.ts src/showcase/useResolvedBrands.ts src/showcase/useResolvedBrands.test.tsx`; and the hero hash command. **Acceptance evidence:** the four focused files pass, the unit stays below 400 changed lines, no generated-code errors remain, and apply progress contains complete phase evidence.

## Work Unit D — all five live native ranges

**Likely files:** `src/showcase/components/ControlPanel.tsx`, `src/showcase/components/Playground.test.tsx`, `src/showcase/model.test.ts`, `src/styles.css`, and slice evidence in `apply-progress.md`.

- [x] **D.RED — Specify native domains and immediate parity.** Replace obsolete invalid numeric-draft expectations with failing table-driven assertions for five `input[type="range"]` controls: Icon width 1–10/0.25/`rem`; Border width 0–8/1/`px`; Duration 5000–120000/1000/`ms`; Gap 0–180/4/`px`; Speed 0.25–4/0.25/`×`. For each available control, `fireEvent.change` must update its `<output>`, latest mocked `Slider` prop, and generated JSX before blur. **Commands:** `pnpm exec vitest run src/showcase/model.test.ts src/showcase/components/Playground.test.tsx`. **Acceptance evidence:** RED fails because current controls are numbers and commit on blur, not because of selector or codegen regressions.

- [x] **D.GREEN — Implement one shared native `RangeField`.** Replace numeric draft/onBlur logic in `ControlPanel.tsx` with native ranges dispatching `event.currentTarget.value` on every change; reuse `NUMERIC_DOMAINS` and reducer normalization; add no custom Arrow/Home/End handler; add only demo-owned range CSS. **Commands:** `pnpm exec vitest run src/showcase/model.test.ts src/showcase/components/Playground.test.tsx`. **Acceptance evidence:** all five controls render exact attributes/units and preview/source parity is immediate.

- [x] **D.TRIANGULATE — Exercise boundaries, variants, reset, and every dataset.** Cover min/max/interior steps, direct out-of-domain/non-finite reducer actions, running-only/fades-only absence, variant round trips, reset defaults, and live `iconWidth` changes for Core/Sport/Food while preserving metadata. Automated tests assert native keyboard capability structurally but do not claim browser Arrow/Home/End behavior. **Commands:** `pnpm exec vitest run src/showcase/model.test.ts src/showcase/components/Playground.test.tsx`. **Acceptance evidence:** normalized values match control/output/package/source, opposite-variant props never leak, and every showcase dataset inherits icon width.

- [x] **D.REFACTOR — Remove numeric-draft duplication and close the unit.** Keep reducer normalization as the sole numeric authority and responsive styles under `.range-field`. **Commands:** `pnpm exec vitest run src/showcase/model.test.ts src/showcase/components/Playground.test.tsx`; `git diff --check`; `git diff --numstat -- src/showcase/components/ControlPanel.tsx src/showcase/components/Playground.test.tsx src/showcase/model.test.ts src/styles.css`; and the hero hash command. **Acceptance evidence:** focused tests pass, no obsolete numeric error/draft path remains, the unit stays below 400 lines, and apply progress records all four stages.

## Work Unit E — deterministic concrete CSS-color resolver

**Likely files:** new `src/showcase/color.ts`, new `src/showcase/color.test.ts`, and slice evidence in `apply-progress.md`.

- [x] **E.RED — Specify the canonical/picker color boundary.** Add failing tests for `ResolvedCssColor`, injected/resolvable concrete colors, trimmed canonical preservation, and an always-`#RRGGBBAA` picker value. Cover `#RGB`, `#RGBA`, `#RRGGBB`, `#RRGGBBAA`, alpha `00/33/ff`, `rebeccapurple -> #663399ff`, `transparent -> #00000000`, rgb/hsl/one modern concrete color sample, invalid/empty values, CSS-wide keywords, `var(`, missing canvas/context/readback, and dual-sentinel disagreement. **Command:** `pnpm exec vitest run src/showcase/color.test.ts`. **Acceptance evidence:** failures show the resolver module is missing; tests do not depend on jsdom's canvas/CSS implementation.

- [x] **E.GREEN — Implement the bounded dependency-free resolver.** In `src/showcase/color.ts`, fast-path valid hex; reject context-dependent inputs; resolve other concrete colors through a temporary document probe; sample a cleared 1×1 default-sRGB canvas from two sentinels; return lowercase eight-digit picker hex while preserving the trimmed canonical string; clean up the probe in `finally`; return `null` instead of guessing. **Command:** `pnpm exec vitest run src/showcase/color.test.ts`. **Acceptance evidence:** the resolver matrix passes without a new dependency and `#00000033` remains exact.

- [x] **E.TRIANGULATE — Prove cleanup, rounding, and failure behavior.** Cover short-hex expansion, omitted alpha, upper/lower input, byte padding, wide-gamut/sample projection contract, assignment failures, thrown readback, sentinel mismatch, probe removal, and no remote image/canvas coupling. **Command:** `pnpm exec vitest run src/showcase/color.test.ts`. **Acceptance evidence:** every success returns concrete canonical plus valid eight-digit picker hex; every unsupported/failure path returns `null` and leaves no probe behind.

- [x] **E.REFACTOR — Isolate browser adapters and close the unit.** Keep the public resolver seam small and test browser operations through injected/stubbed boundaries without creating a general CSS parser. **Commands:** `pnpm exec vitest run src/showcase/color.test.ts`; `git diff --check`; `git diff --numstat -- src/showcase/color.ts src/showcase/color.test.ts`; and the hero hash command. **Acceptance evidence:** tests remain green, no dependency files changed, the unit stays below 400 lines, and apply progress records complete phase evidence.

## Work Unit F — both alpha pickers with precise text fallback

**Likely files:** `src/showcase/components/ControlPanel.tsx`, `src/showcase/components/Playground.tsx`, `src/showcase/components/Playground.test.tsx`, `src/showcase/model.ts`, `src/showcase/model.test.ts`, `src/styles.css`, and slice evidence in `apply-progress.md`.

- [x] **F.RED — Specify two picker/text control pairs.** Mock `react-colorful` with labelled callback seams and add failing assertions for exactly two `HexAlphaColorPicker` controls named `Border color picker` and `Background color picker`; picker props must always be eight-digit hex; callbacks must immediately update text, preview props, and source. Require visible inputs labelled `Border color` and `Background color`, exact invalid feedback `Enter a valid CSS color.`, `aria-invalid`, `aria-describedby`, Enter/blur commit, and last-valid canonical containment. **Commands:** `pnpm exec vitest run src/showcase/color.test.ts src/showcase/model.test.ts src/showcase/components/Playground.test.tsx`. **Acceptance evidence:** RED fails because pickers and resolver integration are absent while current text behavior remains understood.

- [x] **F.GREEN — Integrate the resolver and installed pickers minimally.** Give `Playground` a default `resolveCssColor` seam; derive reducer validation and picker projection from that resolver; render both installed `HexAlphaColorPicker` controls in shared `ColorField`; keep only local text/error draft; dispatch picker changes immediately; commit trimmed valid text on Enter without forced blur and on blur idempotently; keep invalid drafts visible; preserve `#00000033`; add only demo-owned responsive picker CSS. **Commands:** `pnpm exec vitest run src/showcase/color.test.ts src/showcase/model.test.ts src/showcase/components/Playground.test.tsx`. **Acceptance evidence:** both fields synchronize picker/text/preview/source and no non-hex value reaches the picker mock.

- [x] **F.TRIANGULATE — Prove alpha, non-hex, reset, and isolation.** Cover picker alpha values, named/transparent/rgb/hsl precise text via injected resolver, invalid partial text, independent controls, focus retained on invalid Enter, blur, running/fades round trip, uncommitted draft discard on unmount, reset including an invalid default-valued draft, and default `#00000033`. **Command:** `pnpm exec vitest run src/showcase/color.test.ts src/showcase/model.test.ts src/showcase/components/Playground.test.tsx`. **Acceptance evidence:** canonical strings remain exact in preview/source, picker projections are eight-digit hex, invalid drafts cannot replace canonical values, and reset restores both controls.

- [x] **F.REFACTOR — Keep one shared color adapter/field and close the unit.** Remove the obsolete swatch path and duplicate color normalization while retaining the labelled keyboard text fallback. **Commands:** `pnpm exec vitest run src/showcase/color.test.ts src/showcase/model.test.ts src/showcase/components/Playground.test.tsx`; `git diff --check`; `git diff --numstat -- src/showcase/components/ControlPanel.tsx src/showcase/components/Playground.tsx src/showcase/components/Playground.test.tsx src/showcase/model.ts src/showcase/model.test.ts src/styles.css`; and the hero hash command. **Acceptance evidence:** focused tests pass, styles remain demo-owned, dependency hashes match preflight, the unit stays below 400 lines, and apply progress contains all four stages.

## Work Unit G — bounded tokenizer and highlighted read-only viewer

**Likely files:** new `src/showcase/sourceHighlight.ts`, new `src/showcase/sourceHighlight.test.ts`, `src/showcase/components/GeneratedCode.tsx`, new `src/showcase/components/GeneratedCode.test.tsx`, `src/styles.css`, `src/App.test.tsx`, and slice evidence in `apply-progress.md`.

- [x] **G.RED — Specify exact line/token and viewer contracts.** Add failing pure tests for source lines and token kinds `plain/comment/string/keyword/tag/attribute/number/punctuation`, reconstruction equality, sequential numbers, block-comment state, strings/escapes, `<`, `&`, URLs, backticks, tags/attributes, numeric props, punctuation, trailing newline, and long lines. Add component tests for a focusable labelled `<pre><code>`, one `aria-hidden` line-number span per line, non-selectable gutter hooks, token spans, exact visible source, no `contentEditable`/textarea/raw HTML, copy of the original source only, and bounded-overflow CSS hooks. **Commands:** `pnpm exec vitest run src/showcase/sourceHighlight.test.ts src/showcase/components/GeneratedCode.test.tsx`. **Acceptance evidence:** RED fails against the current plain viewer and missing tokenizer for intended reasons.

- [x] **G.GREEN — Implement the bounded scanner and React-node viewer.** Scan only the generated TSX grammar and preserve every character; carry only block-comment state; render explicit rows, line numbers, source spans, and newline text nodes; pass the original source directly to `CopyButton`; use no `dangerouslySetInnerHTML`, `innerHTML`, editor, textarea, execution, save, or persistence path. Add demo-owned `.source-viewer`, line, gutter, content, and `.source-token--*` CSS with local horizontal/vertical overflow. **Commands:** `pnpm exec vitest run src/showcase/sourceHighlight.test.ts src/showcase/components/GeneratedCode.test.tsx`. **Acceptance evidence:** tokenizer and viewer tests pass, copy text equals source byte-for-byte, and presentation never enters the source model.

- [x] **G.TRIANGULATE — Prove safe rendering and source fidelity.** Cover markup-like external metadata, escaped quotes/backslashes, comments, adjacent token kinds, final empty line, very long canonical URLs, generated Sport/Food source, line-number exclusion from clipboard, no editability, and package-root imports. Add/update `src/App.test.tsx` static assertions for `max-width`, bounded `max-height`, `overflow: auto`, `white-space: pre`, `min-width: 0`, and absence of raw HTML APIs in the viewer module. **Commands:** `pnpm exec vitest run src/showcase/sourceHighlight.test.ts src/showcase/components/GeneratedCode.test.tsx src/App.test.tsx`. **Acceptance evidence:** every line reconstructs exactly, line counts match `source.split("\n")`, copied source excludes line numbers/markup, and tests make no visual-selection or contrast claim.

- [x] **G.REFACTOR — Merge adjacent equal-kind tokens and close the unit.** Keep the scanner pure and React-independent; keep copy as the viewer's only action. **Commands:** `pnpm exec vitest run src/showcase/sourceHighlight.test.ts src/showcase/components/GeneratedCode.test.tsx src/App.test.tsx`; `rg -n 'dangerouslySetInnerHTML|innerHTML|contentEditable|<textarea' src/showcase/sourceHighlight.ts src/showcase/components/GeneratedCode.tsx`; `git diff --check`; `git diff --numstat -- src/showcase/sourceHighlight.ts src/showcase/sourceHighlight.test.ts src/showcase/components/GeneratedCode.tsx src/showcase/components/GeneratedCode.test.tsx src/styles.css src/App.test.tsx`; and the hero hash command. **Acceptance evidence:** focused tests pass, the forbidden-API search has no matches, the unit stays below 400 lines, and apply progress records all four stages. If the unit reaches 400, split pure tokenizer/tests from viewer/tests/CSS before lifecycle actions.

## Work Unit H — reusable CopyButton and inline install command

**Likely files:** `src/showcase/components/CopyButton.tsx`, `src/showcase/components/CopyButton.test.tsx`, `src/showcase/components/InstallGuide.tsx`, `src/showcase/components/InstallGuide.test.tsx`, `src/showcase/components/GeneratedCode.tsx`, `src/styles.css`, `src/App.test.tsx`, and slice evidence in `apply-progress.md`.

- [x] **H.RED — Specify reusable composition and inline containment.** Add failing tests for `presentation: "stacked" | "inline-field"` plus caller-supplied children; preserve one clipboard state machine. Require a single `.install-command-field` containing selectable command text and the `Copy install command` button; exact npm text `npm install react-tech-slider`; exact pnpm text `pnpm add react-tech-slider`; selected-tab copying; success; absent/synchronous throw/rejected clipboard; stale request/unmount behavior; and exact failure copy `Copy failed for install command. Select and copy the text manually.` **Commands:** `pnpm exec vitest run src/showcase/components/CopyButton.test.tsx src/showcase/components/InstallGuide.test.tsx src/showcase/components/GeneratedCode.test.tsx`. **Acceptance evidence:** RED identifies the current fixed standalone composition while existing clipboard semantics remain covered.

- [x] **H.GREEN — Compose both copy presentations without duplicating logic.** Extend `CopyButton` with children and presentation; keep generated code stacked with button outside its viewer; render install command text and native button inside one inline field; keep feedback below that row in the same copy block with `aria-live="polite"`; preserve exact accessible names and command constants. Add `minmax(0, 1fr) auto`, locally scrollable/selectable command text, gap/padding, and a minimum 44px target. **Commands:** `pnpm exec vitest run src/showcase/components/CopyButton.test.tsx src/showcase/components/InstallGuide.test.tsx src/showcase/components/GeneratedCode.test.tsx`. **Acceptance evidence:** both consumers pass, only the selected exact command is copied, and failure leaves command/source visible.

- [x] **H.TRIANGULATE — Prove keyboard/tab/status independence and responsive ownership.** Cover npm/pnpm click and keyboard selection, selected-tab status remount, independent generated/install feedback, rapid requests, rejected and missing clipboard, command selectability, one inline button, and static 390px containment hooks. Extend `src/App.test.tsx` so CSS rejects exact private package selectors `.rts-*`, `.wrapper`, `.item`, `.brand-slider` and package animation/alignment overrides while accepting only demo-owned range/picker/viewer/install hooks. **Commands:** `pnpm exec vitest run src/showcase/components/CopyButton.test.tsx src/showcase/components/InstallGuide.test.tsx src/showcase/components/GeneratedCode.test.tsx src/App.test.tsx`; `rg -n '\.(rts-[A-Za-z0-9_-]*|wrapper|item|brand-slider)\b|animation(?:-play-state)?\s*:' src/styles.css`. **Acceptance evidence:** focused tests pass; the search returns no forbidden selector/declaration; package-owned fades geometry remains untouched; tests do not claim pixel overflow from static CSS alone. **Stop:** any need for a private selector, package wrapper/patch, or local alignment workaround blocks apply.

- [x] **H.REFACTOR — Retain one clipboard state machine and close the unit.** Remove obsolete plain-source/install layout rules only after both consumers are green; keep source and install statuses isolated. **Commands:** `pnpm exec vitest run src/showcase/components/CopyButton.test.tsx src/showcase/components/InstallGuide.test.tsx src/showcase/components/GeneratedCode.test.tsx src/App.test.tsx`; `git diff --check`; `git diff --numstat -- src/showcase/components/CopyButton.tsx src/showcase/components/CopyButton.test.tsx src/showcase/components/InstallGuide.tsx src/showcase/components/InstallGuide.test.tsx src/showcase/components/GeneratedCode.tsx src/styles.css src/App.test.tsx`; and the hero hash command. **Acceptance evidence:** focused tests pass, the unit stays below 400 lines, exact copy/status semantics remain, and apply progress contains all four stages.

## Sport fixture amendment — completed before final verification

- [x] **S.RED — Capture the current contract mismatch.** Run the current tests before amendment edits and record failures in `brands.test.ts`, `codegen.test.ts`, and `Playground.test.tsx` caused only by stale ten-record Sport expectations.
- [x] **S.GREEN — Amend directly affected fixture expectations only.** Update `src/showcase/brands.test.ts`, `src/showcase/codegen.test.ts`, and `src/showcase/components/Playground.test.tsx` to assert the authoritative seven-record Sport fixture, `filter: "invert()"`, current class names/order/IDs/URLs, and removal of New Balance/Converse/Asics. Do not edit `src/showcase/brands.ts` or other production files.
- [x] **S.TRIANGULATE/REFACTOR — Preserve adjacent contracts.** Confirm Food remains 10, `MAX_DATASET_SIZE` remains 10, generated Sport metadata retains transition plus filter, Playground preview/source stay synchronized, and the focused amendment suites plus full `pnpm test` pass. Record dependency hashes, protected hero hash, diff integrity, and empty staged set in `apply-progress.md`.

**Boundary:** This amendment does not execute or check any final task below. **10.1 remains the first unchecked task.**

## Final automated verification

- [x] **10.1 Run all focused suites after all work units are integrated.** **Commands:** `pnpm exec vitest run src/showcase/brands.test.ts src/showcase/model.test.ts src/showcase/codegen.test.ts src/showcase/useResolvedBrands.test.tsx`; `pnpm exec vitest run src/showcase/color.test.ts src/showcase/components/Playground.test.tsx`; `pnpm exec vitest run src/showcase/sourceHighlight.test.ts src/showcase/components/GeneratedCode.test.tsx`; `pnpm exec vitest run src/showcase/components/CopyButton.test.tsx src/showcase/components/InstallGuide.test.tsx src/App.test.tsx`. **Acceptance evidence:** every command exits zero and apply progress records suite counts; compiler-API assertions report zero generated-code syntax/type diagnostics.

- [x] **10.2 Run repository verification without substituting it for browser evidence.** **Commands:** `pnpm test`; `pnpm build`; `pnpm lint`; `git diff --check`. **Acceptance evidence:** all four commands exit zero with concise output recorded in `apply-progress.md`; no result is described as proof of pointer behavior, browser accessibility, visual containment, package alignment, or performance.

- [x] **10.3 Prove dependency and scope integrity.** **Commands:** `node -e "const p=require('./package.json'); console.log(p.dependencies['react-tech-slider'], p.dependencies['react-colorful'])"`; `pnpm list react-tech-slider react-colorful --depth 0`; `git hash-object package.json pnpm-lock.yaml`; `git diff -- package.json pnpm-lock.yaml`; `git diff -- src/App.tsx`; `git status --short`; `git diff --name-only`; `rg -n '\.(rts-[A-Za-z0-9_-]*|wrapper|item|brand-slider)\b|animation(?:-play-state)?\s*:' src/styles.css`. **Acceptance evidence:** declared/installed versions are still 1.10.1/5.8.0, manifest/lock hashes and diffs exactly match the recorded preflight state, the only `src/App.tsx` diff is the approved `CORE_BRANDS` import, duplicate local-fixture removal, `brandsList={CORE_BRANDS}` reuse, and `iconWidth={18}` change, no forbidden selector/animation workaround exists, no unrelated file entered scope, and no staged file exists. Any additional drift is a blocker, not cleanup work for this change.

- [x] **10.4 Verify the protected hero with the approved extraction/hash algorithm.** **Command:** `node -e "const {readFileSync}=require('node:fs'); const {createHash}=require('node:crypto'); const s=readFileSync('src/App.tsx','utf8').replace(/\r\n/g,'\n'); const start=s.indexOf('<section className=\"hero-shell\">'); const close='</section>'; const end=s.indexOf(close,start); if(start<0||end<0) process.exit(2); console.log(createHash('sha256').update(s.slice(start,end+close.length)).digest('hex'))"`. **Acceptance evidence:** output is exactly the user-approved final rebaseline `aa787e708db68498c3b051446d7416b9933b2455f238bcf49e346bee3b56bdee`, whose accepted hero Slider uses `iconWidth={18}`; any other output stops final handoff.

## Truthful browser matrix and performance acceptance

**Status:** On 2026-07-28, the user explicitly selected **Waive and finish** for tasks 11.1–11.4. Every browser-only item is individually recorded in `apply-progress.md` as **waived/unverified**, never passed. No browser server, browser session, browser tooling, or other browser/runtime command ran; the waiver provides no rendered evidence and automated results remain non-browser evidence.

- [x] **11.1 Run or explicitly classify the browser matrix.** Likely evidence file: `openspec/changes/refresh-showcase-controls-and-datasets/apply-progress.md`; no production source change belongs to this task. **Command:** `pnpm dev --host 127.0.0.1` plus the available manual/browser tooling. Record separate rows for Chromium, Firefox, and WebKit/Safari where available, each with `passed`, `blocking`, `unverified`, or `waived`—never infer a pass from Vitest/build/lint. **Acceptance evidence:** each browser row states browser/version, viewport, operator/date, and evidence location; unavailable browsers are explicitly unverified or waived. **Closeout:** Chromium, Firefox, and WebKit/Safari are each recorded as waived/unverified under the explicit user waiver; no version, viewport, or rendered observation exists.

- [x] **11.2 Verify ranges, pickers, focus, and exact parity in browsers.** Exercise pointer dragging and Arrow/Home/End on all five ranges; confirm immediate visible preview/output/source parity and icon resizing in Core/Sport/Food. Exercise both `HexAlphaColorPicker` controls and labelled text fallbacks for `#00000033`, named color, transparent, rgb/hsl, one modern concrete color, invalid draft, Enter, blur, reset, and running/fades round trips. **Command:** use the server from task 11.1 and record observations manually. **Acceptance evidence:** every interaction is individually passed/blocking/unverified/waived; focus order, picker keyboard behavior, alpha rendering, and last-valid fallback are described truthfully. A required-browser blocker stops handoff. **Closeout:** Every range, parity, picker, text-fallback, focus, keyboard, alpha, reset, and variant-round-trip item is individually recorded as waived/unverified, never passed.

- [x] **11.3 Verify viewer/install responsive behavior and accessibility observations.** At representative wide width and 390px, inspect line-number alignment, selectable source, gutter exclusion where the browser supports it, long-line local scrolling, contrast, focus visibility, exact generated-code copying, npm/pnpm inline command copying, command selection after clipboard denial, 44px target, and absence of page-level horizontal overflow. **Command:** use the server from task 11.1 and browser accessibility/layout tools. **Acceptance evidence:** each observation is recorded per browser; source/command remain selectable; no edit/execute/save action exists; clipboard failure keeps exact text visible. **Closeout:** Every wide/390px viewer, selection, gutter, scrolling, contrast, focus, clipboard, install-copy, denial-recovery, target-size, and page-overflow item is individually recorded as waived/unverified, never passed.

- [x] **11.4 Verify package-owned fades alignment and bounded dataset behavior.** In Sport and Food, inspect running and fades across desktop/tablet/mobile presets; for fades record the observable 49-image Sport and 100-image Food package trees and test gap `0`/`180`, speed `0.25`/`4`, rapid dataset switches, rapid live controls, containment, alignment, animation responsiveness, memory/network observations, and image fallback appearance. **Command:** use the server from task 11.1 and available browser performance/network panels. **Acceptance evidence:** results are qualitative observations, not a performance guarantee; package-owned fades alignment has no demo workaround; blockers are recorded and escalated; unrun checks remain unverified/waived. **Closeout:** Every Sport/Food, running/fades, preset, image-count, boundary-control, rapid-update, containment, package-owned alignment, responsiveness, memory/network, and fallback item is individually recorded as waived/unverified, never passed.

## Final OpenSpec evidence handoff

- [x] **12.1 Complete per-slice apply evidence.** Update only `openspec/changes/refresh-showcase-controls-and-datasets/apply-progress.md` during apply with each unit's dependency, forecast/actual changed lines, files, RED failure output, GREEN output, TRIANGULATE cases, REFACTOR result, focused command results, diff check, hero hash, and any stop/resolution. **Command:** `git diff --check` and `git diff -- openspec/changes/refresh-showcase-controls-and-datasets/apply-progress.md`. **Acceptance evidence:** all A–H sections are present and truthful; no phase is inferred from a final green run alone; tests/docs are recorded in the same work unit as behavior.

- [x] **12.2 Prepare the final verification handoff without publishing.** Summarize automated commands, compiler diagnostics, browser matrix, performance observations, dependency/scope integrity, actual review size, residual risks, and protected hero hash. **Commands:** `git diff --stat`; `git diff --check`; `git status --short`; `git diff --cached --name-only`. **Acceptance evidence:** the handoff states whether chained delivery or the explicit one-PR `size:exception` was selected, identifies any unverified/waived browser rows, reports no staged files unless separately and explicitly authorized, and recommends review/lifecycle handling rather than performing commits, branches, PRs, release, sync, or archive.

## Phase contract

- **status:** complete
- **executive_summary:** Apply used eight dependency-ordered work units with strict RED → GREEN → TRIANGULATE → REFACTOR evidence. Work Units A–H, the user-approved seven-record Sport fixture amendment, final automated verification, and all task checkboxes are complete. On 2026-07-28, the user explicitly selected **Waive and finish** for browser tasks 11.1–11.4; those tasks are complete only by waiver and every browser-only item remains waived/unverified, never passed. No browser/runtime command ran, and the waiver provides no rendered evidence. The final approved hero baseline remains SHA-256 `aa787e708db68498c3b051446d7416b9933b2455f238bcf49e346bee3b56bdee` with `iconWidth={18}`.
- **artifacts:** `openspec/changes/refresh-showcase-controls-and-datasets/tasks.md`; `openspec/changes/refresh-showcase-controls-and-datasets/apply-progress.md`
- **next_recommended:** Formal SDD verify; do not sync, archive, or publish from apply closeout.
- **risks:** Waived/unverified residual risks remain: native range pointer/Arrow/Home/End; production CSS-color canvas resolver and picker pointer/keyboard/focus/alpha; 390px overflow/selection/gutter/contrast/focus/clipboard denial/44px target; package-owned fades alignment and 49-image Sport/100-image Food responsiveness/network/memory/fallback. Automated verification and the waiver do not establish browser-rendered evidence.
- **skill_resolution:** none
